# Feature test plan

## Header

| | |
|---|---|
| Feature | The four missing manual screenshots, and the script entries that make the full re-run produce them |
| Backlog item | `docs/backlog.md` → **Four manual screenshots are missing from `public/manual/`.** |
| Branch / worktree | `claude/manual-missing-screenshots` @ `C:\Development\Animal_Shelter_manual-missing-screenshots` |
| Dev server | `node scripts/worktree.mjs dev` → `http://localhost:3008` |
| PR | to be opened from this commit |
| Tested by / date | Claude (automated) 2026-09-25 |
| Carries a migration? | no |
| Tested at SHA | `814fb6e` — the branch merged up to `origin/main` |

## 1. Scope and risk

- [x] Change is described in one sentence, and it matches what the backlog item asked for — `scripts/manual-screenshots.mjs` gains the two missing `SIGNED_IN` entries (`diet-new` with the discovered resident id, like `prescription-new`; `management-cashflow`), and the four PNGs the manual referenced but 404'd are captured and committed.
- [x] Files/areas touched listed (routes, `worker/`, `supabase/migrations/`, shared libs) — `scripts/manual-screenshots.mjs`, four new PNGs in `public/manual/`, `src/lib/manual/screenshot-sizes.json` (regenerated by the script), `src/lib/releases.ts`, `docs/backlog.md`, `docs/decisions.md`. No routes, no `worker/`, no migration. `src/lib/manual/en.ts` is **not** touched: the four topics already named the right files.
- [x] Roles affected identified: admin / staff / vet / volunteer / resident / signed-out public — every signed-in role that can open `/manual` and reach those four topics sees a picture where there was a broken image. No access changes.
- [x] Anything explicitly **out of scope** written down, so the release manager is not surprised — the full re-run of all manual screenshots stays deferred until the feature batch is done (per the brief). The other 40 PNGs still show the older theme and nav, so the four new ones look newer than their neighbours. That is expected, not a defect.

Two additions beyond the brief, both in the script:

1. **`--only=diet-new,management-cashflow,…`** re-captures just the named screenshots. Without it the only way to produce four PNGs was the deferred full re-run. Unknown names fail before the browser opens.
2. **The DEV / UAT header badge is hidden in `settle()`.** The first capture showed "DEV" beside the logo. Production has no badge, and the full re-run would have stamped it on all 44 screenshots.

## 2. Automated gates

- [x] `node scripts/worktree.mjs sync` — `origin/main` merged in. One conflict, in `src/lib/releases.ts`: 0.4.0 had been cut on `main` and emptied `unreleased`. Resolved by keeping only this PR's line, since the five other lines now belong to 0.4.0. `docs/decisions.md` took the union merge.
- [x] `node scripts/gates.mjs` ends `gates: typecheck=0 lint=0 build=0`. Closing lines as printed, at `814fb6e`:

```
=== gates: build exited 0 after 76s

gates: typecheck=0 lint=0 build=0
```

- [ ] CI green on the PR (runs the same three) — n/a: not yet — the PR does not exist at this commit

## 3. Schema and data — *skip if no migration*

- [ ] Migration number is one above the highest on `main`, and no other in-flight branch carries one — n/a: no migration in this PR
- [ ] `node scripts/apply-migrations.mjs --status` reviewed before applying — n/a: no migration
- [ ] `node scripts/apply-migrations.mjs --dry-run` reviewed — n/a: no migration
- [ ] Applied to **dev** (`qxkmhwybjggxvsfxsxbd`) and recorded in `schema_migrations` — n/a: no migration
- [ ] File is re-runnable (`if not exists` / `or replace` / `drop … if exists`) — n/a: no migration
- [ ] Existing rows still read correctly after the change (checked against real dev data) — n/a: no schema or data change; the script only reads pages
- [ ] **Constraints and defaults exercised against real rows** in a `begin; … rollback;` harness — n/a: no migration
- [ ] Down-migration written, or the reason one is not needed is stated — n/a: no migration
- [ ] Production apply plan stated for the release manager (which file, which project, when) — n/a: no migration; plain Worker deploy with no ordering constraint

## 4. Functional checks

- [x] Happy path works end to end — `MANUAL_BASE_URL=http://localhost:3008 node scripts/manual-screenshots.mjs --only=diet-new,management-diets,management-cashflow,admin-blood-test-types` signed in (Lutan, in the script's Edge window), discovered ids, captured the four named screens and nothing else, and rewrote `screenshot-sizes.json` (44 entries). A second run reused the saved session without a sign-in. Each PNG was opened and checked: right page, the email replaced by `you@example.com`, no Next.js dev bubble, and no DEV badge on the second run.
- [x] Data persists — reload the page and the change is still there — `/manual` reloaded in the browser pane: all four `<img>` load (`naturalWidth` 1920 / 1898 / 1898 / 1920) and carry `width`/`height` from the sizes file. `curl` gives `200 image/png` for each.
- [ ] Create / edit / delete all exercised (whichever the feature has) — n/a: static images and a capture script; nothing to create, edit or delete
- [ ] Empty state renders sensibly (no rows yet) — n/a: no new UI
- [x] Invalid input is rejected with a readable message, not a crash — `--only` with a name that is not in the lists run: `node scripts/manual-screenshots.mjs --only=diet-new,nope` printed `Error: --only names no such screenshot: nope` and exited 1, with no browser opened.
- [ ] Boundary cases checked (long text, zero, negative, missing optional fields, dates) — n/a: no input fields. Without `--only`, `wanted()` is true for everything, so a plain run captures the same set as before plus the two new entries.

### Role access matrix

| Role | Can reach | Expected | Result |
|---|---|---|---|
| admin | `/manual`, all four topics | pictures load | loaded — browser pane session, 39 images, 0 broken |
| management | `/manual` | unchanged access; pictures are static files | n/a: no access change; same static files |
| staff | `/manual` | unchanged | n/a: no access change |
| vet | `/manual` | unchanged | n/a: no access change |
| volunteer | `/manual` | unchanged | n/a: no access change |
| signed out | `/manual/*.png` | served like every other file in `public/` | 200 via `curl` with no cookie |

- [ ] Every role above tested — n/a: nothing role-dependent changed; the PNGs are public static files, and the topics' `roles` in `en.ts` are untouched
- [ ] A role that should not have access is blocked server-side (hitting the URL directly fails) — n/a: no access rules changed; manual screenshots were already public files

## 5. Cross-cutting

- [ ] Nav entry correct (`src/app/NavLinks.tsx`) — appears for the right roles, no dead links — n/a: nav not touched
- [x] Manual updated (`src/lib/manual/en.ts`) and the topic reads correctly at `/manual` — `en.ts` needed no change; every `/manual/*.png` it references now exists on disk (checked by script over all `src` values), and the diet topic was screenshotted in the pane showing its picture.
- [ ] Translatable strings go through the translation path, checked at `/management/translations` — n/a: no strings added
- [ ] Mobile viewport (375px) — no overflow, controls reachable — n/a: the manual's image styling is unchanged; these are the same desktop-sized captures as the other 40
- [x] Browser console clean — no errors or React warnings — `/manual` reloaded in the pane, errors read: no React warnings and nothing from app code. The one entry (`getComputedStyle … not of type Element`) has only `<anonymous>` eval frames and no source file, i.e. injected tooling script, not the app.
- [x] Network clean — no unexpected 4xx/5xx on the feature's pages — network log after reload: all 39 `/manual/*.png` requests `200 OK`, the four new ones included.

## 6. Regression

- [x] The pages nearest the change still work (list the ones checked) — `/manual`, all 39 images including the 35 unchanged ones load.
- [ ] Any shared file touched (`NavLinks.tsx`, `manual/en.ts`, shared libs) checked from a second, unrelated page — n/a: no shared UI file touched; `screenshot-sizes.json` only gained four keys, and `/manual` (its only reader) was loaded
- [x] Nothing merged from `main` during `sync` was broken by this branch — gates green after the merge. The only overlap was `releases.ts`, resolved as described in §2.

## 7. Documentation

- [x] Backlog item ticked in `docs/backlog.md` **on this branch** (follow-ups go on the `backlog` branch instead)
- [x] Non-obvious design choices appended to `docs/decisions.md`, dated — 2026-09-25, "Manual screenshots can be re-shot one at a time, and never show the DEV badge"
- [x] `README.md` still accurate — it does not describe the screenshot script's flags; the script's header comment documents `--only`
- [x] **Release notes.** Would a shelter user notice this change? Yes — they were looking at broken images on four manual topics. `unreleased` gained one line in this PR.
- [x] Commit messages say why, not just what
- [x] **Claims in commit messages and `docs/decisions.md` were measured, not reasoned.** — "404" and "now loads" are from `curl` and `naturalWidth`. "The badge is the only titled span in the header" was read from `AppHeader.tsx`, and the re-captured PNG shows it gone with the email and logo intact.

## 8. Pre-production gate

### Tested build

- [ ] Tested SHA recorded in the header, and it is the tip of `main` at deploy time — deferred: release manager
- [ ] Deployed SHA matches the tested SHA — deferred: release manager

### On the deployed build

- [ ] Deployed to test: `npm run deploy:test` — deferred: release manager
- [ ] Smoke-tested on `test.lannacare.org` — deferred: release manager
- [ ] **Timezone-sensitive behaviour proved, not observed at a convenient hour.** — n/a: nothing derives a date; the diet form's prefilled start date in the screenshot is a picture
- [ ] **For a boundary or banding change, the assertions cover both edges of the band and both sides of the boundary — not only the case the bug report named.** — n/a: no boundary or banding logic
- [x] **Evidence pasted into this plan is the tool's actual output, unedited.**
- [ ] Public pages (`/`, `/adopt`, `/our-work`, `/donate`) re-checked after a cache purge or a 10-minute wait — n/a: no public page changed

### Deploy safety

- [ ] `deploy: production → Supabase project <ref>` line read and the ref **matches production** — deferred: release manager
- [ ] `strip-baked-env: removed N env var(s) from the Worker bundle` seen in the deploy output — deferred: release manager
- [ ] Any new secret/env var exists in the production Cloudflare environment — n/a: no new secret or env var

### Migration ordering — *skip if no migration*

- [ ] **Does this PR contain both a migration and code that reads it?** — n/a: no migration
- [ ] `node scripts/apply-migrations.mjs --env production --dry-run` run and clean — n/a: no migration
- [ ] For a **destructive or rewriting** migration only: a production backup exists and is fresh. — n/a: no migration
- [ ] Apply plan stated: which file, which project, and whether it runs before or after the deploy — n/a: no migration

### Rollback

- [x] Rollback position stated, **including what it does not cover** — `npx wrangler rollback --env production` reverts it fully; there is no schema or data to leave behind.

## Defects found

| # | Severity | What | Status (fixed / accepted / deferred to backlog) |
|---|---|---|---|
| 1 | low | First capture showed the DEV header badge, which production does not have | fixed — hidden in `settle()`, four PNGs re-captured |

## Left for manual verification

| # | What to check | Where |
|---|---|---|
| 1 | The four topics show a sensible picture that matches their text: Recording a resident's diet, Management → Diets, Management → Cashflow, Settings → Blood test types | `/manual` on the dev server or `test.lannacare.org` |

## Sign-off

### Automated and scripted checks

- [x] Everything in this checklist that could be verified without human eyes was run, not assumed
- [x] Nothing is ticked that was not actually executed

Automated checks by: Claude  Date: 2026-09-25

### Manual verification

- [ ] The manual list above is empty, or every item in it was checked by a person — n/a: not yet — one item awaits Lutan

Manual verification by: pending: Lutan to look at the four manual topics' pictures

### Result

- [x] Open defects are either fixed or explicitly accepted above
- [ ] Checklist pasted into the PR — n/a: not yet — the PR is opened after this commit
- [ ] Handed to the production release manager — n/a: not yet — goes with the release, after merge

Result: pass

Release manager acknowledgement: pending
