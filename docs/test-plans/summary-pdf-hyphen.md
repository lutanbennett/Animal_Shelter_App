# Feature test plan

Filled from `docs/test-plan-template.md`; see it for how each line is judged.

---

## Header

| | |
|---|---|
| Feature | Summary PDF: no stray hyphen where a long name wraps before a Thai word |
| Backlog item | `docs/backlog.md` → Resident operations: "Summary PDF: a stray hyphen where a long name wraps before a Thai word." |
| Branch / worktree | `claude/summary-pdf-hyphen` @ `C:\Development\Animal_Shelter_summary-pdf-hyphen` |
| Dev server | `node scripts/worktree.mjs dev` → `http://localhost:3005` (not used: the PDF was rendered directly, see §4) |
| PR | #82 |
| Tested by / date | Claude (summary-pdf-hyphen session), 2026-09-24 |
| Carries a migration? | no |
| Tested at SHA | `2f2ca39` (synced `main`, #80) plus this PR's diff to `src/lib/archive/resident-summary-pdf.tsx` |

## 1. Scope and risk

- [x] Change is described in one sentence, and it matches what the backlog item asked for: every `Text` in the summary PDF carries `hyphenationPenalty` 10000 (textkit's infinity), so the line breaker can no longer break, and insert `-`, at the join between two script runs such as `(` and a Thai name
- [x] Files/areas touched listed: `src/lib/archive/resident-summary-pdf.tsx` (a `Text` wrapper shadowing @react-pdf's), `src/lib/releases.ts`, `docs/backlog.md`, `docs/decisions.md`, this plan. No route, `worker/` or migration
- [x] Roles affected identified: none change. The PDF is generated server-side when admin/staff record a death, edit a deceased resident's bio or photos, or press Retry; who can do those is untouched
- [x] Out of scope written down: the six production summary PDFs are **not** regenerated by this PR. That is its own backlog item, a production operation and Lutan's, and it is now due once this is deployed. Also out of scope: a single unspaced string wider than the line still gets a hyphen from textkit's best-fit fallback (measured, §4; `docs/decisions.md`)

## 2. Automated gates

- [x] `node scripts/worktree.mjs sync` — `origin/main` merged in cleanly (fast-forward to `2f2ca39`, pushed)
- [x] `node scripts/gates.mjs` ends `gates: typecheck=0 lint=0 build=0`. Closing lines as printed:

```
=== gates: build exited 0 after 216s

gates: typecheck=0 lint=0 build=0
```

- [x] CI green on the PR: run 35941705661 on `0835842` passed, `check` and `test-plan` both green

## 3. Schema and data — *skip if no migration*

- [ ] Migration number is one above the highest on `main`, and no other in-flight branch carries one — n/a: no migration
- [ ] `node scripts/apply-migrations.mjs --status` reviewed before applying — n/a: no migration
- [ ] `node scripts/apply-migrations.mjs --dry-run` reviewed — n/a: no migration
- [ ] Applied to **dev** (`qxkmhwybjggxvsfxsxbd`) and recorded in `schema_migrations` — n/a: no migration
- [ ] File is re-runnable (`if not exists` / `or replace` / `drop … if exists`) — n/a: no migration
- [ ] Existing rows still read correctly after the change (checked against real dev data) — n/a: no migration
- [ ] **Constraints and defaults exercised against real rows** in a `begin; … rollback;` harness — n/a: no migration
- [ ] Down-migration written, or the reason one is not needed is stated — n/a: no migration
- [ ] Production apply plan stated for the release manager (which file, which project, when) — n/a: no migration

## 4. Functional checks

Method: a scratch harness bundled `renderResidentSummaryPdf` (the real export) with esbuild and rendered fixed records to PDF under Node, before and after the change. Headings read with `pdftotext -layout`, and the pages looked at, rendered by pdf.js in the browser pane.

- [x] Happy path works end to end: `Princess Buttercup of the Misty Valley (เจ้าหญิงบัตเตอร์คัพแห่งหุบเขา)` printed `…Misty Valley (-` / `เจ้าหญิง…)` before and `…Misty Valley` / `(เจ้าหญิง…)` after, both confirmed by eye. The longer `…Misty Mountain Valley (…)` case likewise lost its `(-`
- [ ] Data persists — reload the page and the change is still there — n/a: no data is written; the PDF is a pure render of the record
- [ ] Create / edit / delete all exercised (whichever the feature has) — n/a: rendering only, no CRUD
- [ ] Empty state renders sensibly (no rows yet) — n/a: layout of empty sections is untouched; the test record's empty sections (placements, immunizations, …) still read "No … recorded." in the after render
- [ ] Invalid input is rejected with a readable message, not a crash — n/a: no input; the PDF renders whatever the record holds
- [x] Boundary cases checked: a long Latin name (`Wolfeschlegelsteinhausenbergerdorff Supercalifragilisticexpialidocious Honorificabilitudinitatibus`) wraps at spaces with no hyphen, before and after; names where the break falls before `Road (` and `Roads End (` were unaffected before and after; the bio's inline Thai phrase and the footer (`… · DEV` and `1 / 2`) render; an unspaced `OverflowName(` + 68 Thai characters, wider than the line, still prints `OverflowName(-` via textkit's best-fit fallback, recorded as accepted defect 1

### Role access matrix

| Role | Can reach | Expected | Result |
|---|---|---|---|
| admin | n/a | access unchanged | n/a: no route or permission touched |
| management | n/a | access unchanged | n/a: no route or permission touched |
| staff | n/a | access unchanged | n/a: no route or permission touched |
| vet | n/a | access unchanged | n/a: no route or permission touched |
| volunteer | n/a | access unchanged | n/a: no route or permission touched |
| signed out | n/a | access unchanged | n/a: no route or permission touched |

- [ ] Every role above tested — n/a: server-side PDF styling only; no role sees anything different except the PDF itself
- [ ] A role that should not have access is blocked server-side (hitting the URL directly fails) — n/a: no route touched

## 5. Cross-cutting

- [ ] Nav entry correct (`src/app/NavLinks.tsx`) — n/a: not touched
- [ ] Manual updated (`src/lib/manual/en.ts`) — n/a: the manual does not describe the PDF's line breaking
- [ ] Translatable strings go through the translation path — n/a: no strings added; the PDF is English-only by design
- [ ] Mobile viewport (375px) — n/a: a fixed A4 PDF, no viewport
- [ ] Browser console clean — n/a: no page changed
- [ ] Network clean — n/a: no page changed

## 6. Regression

- [x] The pages nearest the change still work: the whole rendered summary (header, Death box, Identity, Bio & background, empty sections, footer on both pages) looked at after the change
- [ ] Any shared file touched checked from a second, unrelated page — n/a: no shared runtime file touched; `releases.ts` gained one string and the build (which bundles it) passed
- [x] Nothing merged from `main` during `sync` was broken by this branch: the sync was a fast-forward and the gates ran after it

## 7. Documentation

- [x] Backlog item ticked in `docs/backlog.md` **on this branch** (moved to Completed → Resident operations)
- [x] Non-obvious design choices appended to `docs/decisions.md`, dated: why the callback alone could not work, why a penalty, and what it does not cover
- [x] `README.md` still accurate (it does not describe PDF line breaking)
- [x] **Release notes.** `unreleased` gained "In that PDF, a long name followed by a Thai name no longer gets a stray hyphen where it wraps onto a second line." Rare, but it is in the permanent record, and staff reading a long-named resident's summary would see it
- [x] Commit messages say why, not just what
- [x] **Claims in commit messages and `docs/decisions.md` were measured, not reasoned.** The run-split cause was read in `@react-pdf/textkit` 7.0.1 (`wrapWords`, `getNodes`, `breakLines`) and confirmed by the before/after renders; the fallback caveat was measured with the overflow case above

## 8. Pre-production gate

### Tested build

- [ ] Tested SHA recorded in the header, and it is the tip of `main` at deploy time — deferred: release manager
- [ ] Deployed SHA matches the tested SHA — deferred: release manager

### On the deployed build

- [x] Deployed to test: `node scripts/deploy.mjs --env test` from this branch, exit 0; it printed `deploy: test → Supabase project qxkmhwybjggxvsfxsxbd (0835842)`, `strip-baked-env: removed 10 env var(s)` and Worker version `c858493b-8e2f-4f66-9495-e98d732fe6d0`. The `Failed to copy color-string / data-uri-to-buffer` lines are the known cosmetic ones (README, decisions.md)
- [x] Smoke-tested on `test.lannacare.org`: the site answered 200 and `/api/releases/current` returned 0.1.0; Lutan regenerated a long-named resident's summary on the Workers build and the heading broke correctly (manual item 1)
- [ ] **Timezone-sensitive behaviour proved** — n/a: nothing here derives a date
- [ ] **For a boundary or banding change, both edges covered** — n/a: not a threshold change; the one boundary (lines wider than the page) is covered in §4
- [x] **Evidence pasted into this plan is the tool's actual output, unedited** (the gates lines)
- [ ] Public pages re-checked after a cache purge — n/a: no public page touched

### Deploy safety

- [ ] `deploy: production → Supabase project <ref>` line read and matches production — deferred: release manager
- [ ] `strip-baked-env` line seen in the deploy output — deferred: release manager
- [ ] Any new secret/env var exists in the production Cloudflare environment — n/a: none added

### Migration ordering — *skip if no migration*

- [ ] Does this PR contain both a migration and code that reads it? — n/a: no migration
- [ ] `apply-migrations.mjs --env production --dry-run` — n/a: no migration
- [ ] Production backup for a destructive migration — n/a: no migration
- [ ] Apply plan stated — n/a: no migration

### Rollback

- [x] Rollback position stated: `npx wrangler rollback --env production` reverts it completely; there is no schema. PDFs regenerated while this was live keep their (correct) line breaks

## Defects found

| # | Severity | What | Status (fixed / accepted / deferred to backlog) |
|---|---|---|---|
| 1 | low | A single unspaced string wider than the line (e.g. a very long Thai phrase with a script change inside it) still gets a hyphen from textkit's best-fit fallback, which ignores the penalty's meaning | accepted: pre-existing, not made worse, needs a textkit patch; recorded in `docs/decisions.md` |

## Left for manual verification

| # | What to check | Where |
|---|---|---|
| 1 | On the Workers build, regenerate a dev deceased resident's summary whose name plus Thai name is long enough to wrap in the heading (e.g. edit its bio to trigger regeneration), open the PDF from Drive and confirm the heading breaks before `(` with no `-` | `test.lannacare.org` after `npm run deploy:test` |

## Sign-off

### Automated and scripted checks

- [x] Everything in this checklist that could be verified without human eyes was run, not assumed
- [x] Nothing is ticked that was not actually executed

Automated checks by: Claude (summary-pdf-hyphen session)  Date: 2026-09-24

### Manual verification

- [x] The manual list above is empty, or every item in it was checked by a person: item 1 checked by Lutan on test.lannacare.org

Manual verification by: Lutan Bennett — confirmed in chat; line written by Claude at their request  Date: 2026-09-24

### Result

- [ ] Open defects are either fixed or explicitly accepted above — n/a: not yet — defect 1 is accepted by Claude, pending Lutan's agreement
- [x] Checklist pasted into the PR (#82 description)
- [ ] Handed to the production release manager — n/a: not yet — at the next release, not part of this PR

Result: pass with accepted defects

Release manager acknowledgement: n/a: not yet handed over
