# Release record — 2026-09-24

Three releases shipped on this date. The per-release pass in
`docs/release-smoke-test.md` is written for one release; this record covers all
three, because the convention names the file by date and three landed in a day.

**What this record is honest about:** everything below was verified by script or
by HTTP request. The smoke test's **browser** items — the Dev badge and palette,
signing in with Google, signing in with email and password, and confirming
`x-lanna-cache: HIT` on a second load — were **not** done. A `curl` cannot check
them: it sends no `locale` cookie, so it always sees `BYPASS`, and it cannot sign
in. Those items are listed as outstanding rather than ticked. Nobody has looked at
this release in a browser.

## The three releases

| Version | Commit | Production Worker | Test Worker | Mail |
|---|---|---|---|---|
| `0.1.0` | `84e61e0` | `fded3541` | not deployed — rule did not exist yet | sent 1, skipped 1 |
| `0.2.0` | `14f183f` | `0791a995` | `2e310d91` | sent 1, skipped 1 |
| `0.2.1` | `2b706f5` | `eae5da6f` | `44f2f6e2` | none — `major: false` |

Production schema ended the day at **76 applied, 0 pending**.

## Before each deploy

- [x] `main` green and the SHA known — CI passed on each of `84e61e0`, `14f183f`, `2b706f5` before deploying
- [x] Every PR in the release had a completed `docs/test-plans/<feature>.md`, checked by hand — 24 plans by the end of the day, none with `Result: fail`
- [x] Migrations applied **before** the deploy where code in the release read them — `0071`, `0072`, `0074`, `0075`, `0076` all applied ahead of their deploy. `0075` mattered most: `src/app/management/contacts/` already read `archived_at`, so deploying first would have broken Management → Contacts

## During each deploy

- [x] `deploy: production → Supabase project dbkodyyxxhtygxcxmfcu (<sha>)` read, not assumed — ref was production and the SHA matched on all three
- [x] `strip-baked-env: removed 10 env var(s)` present on every deploy, production and test

## The app

- [x] Version endpoint reports the expected release — `/api/releases/current` returned `0.1.0`, `0.2.0`, `0.2.1` in turn
- [x] `/` and `/login` return 200
- [x] Staff-only routes refuse a signed-out request server-side — `/releases` and `/management/cashflow` both 307 to login
- [ ] No Dev badge, palette is orange — **not checked: needs a browser**
- [ ] Sign in with Google works — **not checked: needs a browser and an account**
- [ ] Sign in with email and password works — **not checked: needs a browser and an account**

## Signed out

- [x] `/adopt` returns 200 signed out
- [x] Public views readable but not writable by `anon` — `check-public-views.mjs` passed after every migration, including the two new `shelter_friends` rows, where the base table refuses `anon` (401) so the view is the only way in
- [ ] Public pages re-checked after a cache purge, and `x-lanna-cache: HIT` on a second load — **not checked: `curl` sends no `locale` cookie so it always sees `BYPASS`; this one genuinely needs a browser**

## Date handling

- [ ] Anything showing "today" is correct for Thailand — **not re-checked here.** It was verified for `0.1.0` by Lutan himself, overnight on 2026-09-23/24, which is what `docs/test-plans/utc-today.md` records with his signature. That is the check that mattered; it has not been repeated since

## Test gets the same release

- [x] Checked what was on test before overwriting it, every time
- [x] Same commit to both — `14f183f` and `2b706f5` each went to test and then production
- [x] `deploy: test → Supabase project qxkmhwybjggxvsfxsxbd (<sha>)` — dev project, SHA matching production's
- [x] Both environments report the same version — verified after each release
- [x] A test deploy mails nobody — confirmed twice, by two different mechanisms: `0.2.0`'s test deploy printed `[off]: sent 0, skipped 2` (the environment guard refused it), and `0.2.1` printed nothing at all (`major: false`, so the step never engaged)

`0.1.0` predates the rule and went to production only; test sat on `0.0.1` until `0.2.0`, which is what prompted the rule.

## Result

Result: **pass** — with the browser items above outstanding, not waived

Run by: Claude (release manager session)  Date: 2026-09-24
Browser and sign-in items: **not run** — no person has opened any of these three releases in a browser

## What broke, if anything

Nothing broke in production. Four things went wrong on the way, all caught before
they shipped:

| What | Where | Action taken |
|---|---|---|
| `deploy:prod` could not deploy at all — a release title with spaces broke wrangler's argument parsing via OpenNext delegation | `scripts/deploy.mjs` | Fixed in #63 with `--autoconfig false`; `0.1.0` was deployed by hand with the same flag first |
| An interrupted OpenNext build left `@react-pdf/reconciler` an empty directory, and `npm ci` no-opped because the package's `package.json` survived | `node_modules`, twice | Purged and reinstalled; both the cause and the misleading audit recorded in README |
| `0075`/`0076` dry-run reported `0076` as FAILED when the file was correct — each file dry-runs in its own `begin…rollback`, so it could not see `0075`'s new column | `scripts/apply-migrations.mjs --dry-run` | Diagnosed by reading both files, then applied: a real apply commits `0075` first and both succeeded. Now written into CLAUDE.md's migrations section |
| `test-plan` reported green even when the checklist was broken, because the job carried `continue-on-error: true` | `.github/workflows/ci.yml` | Fixed in this PR: the script now exits 0 for a correct-but-unsigned plan and non-zero for a broken one, so the suppression is no longer needed |

## Findings for next time

- **The record convention collides.** `docs/releases/<date>.md` assumes one release
  a day and three landed today. Either the filename needs the version
  (`2026-09-24-v0.2.1.md`) or the file needs to be a day's log, as this one is.
- **A release record written by a script is not a smoke test.** Everything a
  `curl` can reach is ticked above and everything it cannot is outstanding. If the
  browser items matter — and the cache one does, since the edge cache is what keeps
  public pages off the CPU-limited render path — they need a person, and the record
  should stay honest about that rather than quietly ticking what was inferred.
