# Release record — 2026-09-26

One release on this date: `0.6.0`, "A new public website, My tasks as your home
page, and Stocktake". `major: true`, so the release mail ran.

**Written late, on 2026-09-27, and this record says plainly what that costs.**
The production apply and deploy were run by Lutan — the auto-mode classifier
refuses both `[Production Reads]` and `[Production Deploy]` from a session — and
the deploy output was never pasted back before the session moved on. So the four
lines the smoke test asks to *read* in that output (the project ref, the SHA,
`strip-baked-env`, and the release mail counts) **were not captured and cannot be
reconstructed**. Everything ticked below was verified over HTTP against the live
site afterwards. Nothing here is inferred from the deploy having "obviously
worked".

| | |
|---|---|
| Release date | 2026-09-26 |
| Deployed SHA | `8cc4880` at the cut; **not confirmed against the deploy output** |
| PRs in this release | #133–#143 (eleven), cut by #145, with #144 dropping one note first |
| Migrations applied | `0091_record_stocktake_staff.sql` only |
| Run by | apply and deploy: Lutan Bennett. Preparation, test deploy, verification and this record: Claude (release manager session) |

## Before the deploy

- [x] `main` green and the SHA known — all three CI checks passed on #145; merged as `8cc4880`
- [x] Every PR has a completed test plan, checked by hand — all eleven feature branches since `0.5.0` have one under `docs/test-plans/`. None reports `Result: fail`; two report `pass with accepted defects`
- [x] Migrations applied before the deploy — see below

## The migration count was wrong, and the plan is what caught it

The cut PR's apply plan stated that **five** migrations (`0087`–`0091`) were
pending on production, reasoning from production having been at `0086` after
`0.5.0`. That reasoning was wrong: the schema streams had applied their own
migrations to production as they merged during the day.

`--drift production`, run by Lutan against `origin/main 8cc4880`, answered:

```
91 file(s), 90 applied row(s).
  On origin/main, not applied here: 1
    0091_record_stocktake_staff.sql
  Applied here, no file on origin/main: 0
Drift: production and origin/main disagree.
```

**One file, not five.** The plan said "confirm production is at `0086` before
assuming five are pending", and that instruction is the only reason the wrong
number did no harm. Worth keeping as the pattern: a release manager reasoning
about production's schema from the last release's record is reasoning from a
snapshot that other streams have been writing to all day. The measurement is
cheap; the assumption is what costs.

`0091` is a single `create or replace function`, re-runnable, with nothing else
pending for it to depend on — so the two-file dry-run artefact could not apply,
and a clean dry-run was the expectation.

### What `0091` changes, since it is a privilege change

It widens `record_stocktake` from admin and management to also admit staff and
volunteers, which is what note 10 promises. To do that it becomes
`security definer`, so RLS no longer filters it. Read rather than waved through:
the body writes only `stock_on_hand`, the role check at the top is now the whole
access rule and explicitly refuses vets, `public_viewer`, anon and archived or
role-less logins, and `set search_path = public` is pinned — that pin is what
keeps a definer function safe from a caller's search path. Grants unchanged, anon
and public revoked.

**Deploying without it would not have broken anything**, which is worth recording
because the cut PR braced for worse: Stocktake would have worked for admin and
management and refused staff and volunteers. A degradation against the release
note, not a broken page.

## During the deploy

- [ ] `deploy: production → Supabase project dbkodyyxxhtygxcxmfcu (<sha>)` — **not captured.** Run by Lutan; output not kept
- [ ] `strip-baked-env: removed N env var(s) from the Worker bundle` — **not captured.** Second release running that this line is missing, and it is the one line standing between a Worker bundle and baked-in credentials
- [ ] `deploy: release mail for 0.6.0 [UAT]: sent N, skipped M` — **not captured.** So it is **unknown** whether the shelter admin address was ever verified under Email Routing (`docs/email-sending.md` §1b), which was the open question from `0.5.0`. It stays open

## The app on `lannacare.org`

Verified over HTTP on 2026-09-27, cache-busted:

- [x] Version endpoint reports `0.6.0` — and the endpoint is `no-store`, so it cannot be a stale answer
- [x] **The sign-in lock still works** — `/` returns 200 and renders the "Staff testing site" page with a Sign in button; `/adopt` 307s to it
- [x] **The new routes exist and are gated** — `/stocktake` and `/my-tasks` both 307 to login rather than 404, so the release's two new features are deployed and behind auth
- [x] Staff-only routes refuse a signed-out request — `/residents` and `/admin` 307
- [ ] No Dev badge, palette orange — **not checked: needs a browser**
- [ ] Both sign-in paths — **not checked: needs a browser and an account**
- [ ] **The two changes most likely to generate support questions** — signing in landing on My tasks instead of Residents, and archived or role-less accounts being refused at password sign-in — **not checked.** Both need a signed-in session, and both are the reason this release was mailed to admins
- [ ] Stocktake saving as a staff or volunteer account — **not checked**, and this is the one `0091` exists for

## Signed out

- [x] `/` renders the lock page — 200
- [x] `/adopt` and `/donate` 307 to the lock, as designed since `0.5.0`
- [ ] The redesigned public pages themselves — **not checked, and not checkable signed out.** This release redesigns every public page and the sign-in lock hides all of them from an anonymous request. That is the intended behaviour and it also means **the largest visual change this project has shipped is unverified on production by anyone**

## Date handling

- [ ] Anything showing "today" is correct for Thailand — **not checked, and it matters more than usual this time.** Stocktake records counts dated today, and My tasks buckets jobs into overdue / due today / coming up. Both are new and both are date-sensitive in a way the previous two releases were not

## Test gets the same release

- [x] Checked what was on test first — `curl` showed `0.5.0`, not a bespoke integration build
- [x] `npm run deploy:test` from the same commit — `8cc4880`, deployed by Claude before production
- [x] `deploy: test → Supabase project qxkmhwybjggxvsfxsxbd (8cc4880)` — dev project, read in the output
- [x] `strip-baked-env: removed 10 env var(s)` — captured on the **test** deploy, so the mechanism is known to work; only production's line is missing
- [x] Both report `0.6.0`
- [x] A test deploy mails nobody — `deploy: release mail for 0.6.0 [off]: sent 0, skipped 2`

Deploying test first paid off in the usual way: test runs the dev database, which
already had all of `0087`–`0091`, so the new code was exercised against a correct
schema before the same question was put to production.

## Result

Result: **pass** — with the deploy output uncaptured and the browser and
signed-in items outstanding, not waived

Run by: Lutan Bennett (apply and deploy) and Claude (preparation, test deploy, verification, record)  Date: 2026-09-26, recorded 2026-09-27

## What broke, if anything

Nothing broke. Two process misses:

| What | Where | Action taken |
|---|---|---|
| The release record was not written on the day. `0.6.0` reached production and sat there with no record until the next release's preparation noticed the missing file | `docs/releases/` | Written now, on 2026-09-27, honest about what could no longer be captured. The gap was found by the next release's pre-flight, which is the check working — but a release manager should not need the following release to notice |
| The deploy output was not captured, for the second release running | `npm run deploy:prod` | The advice to redirect to `deploy.log` was given and the file was not pasted back. Three smoke-test lines are permanently unrecorded for this release |

## Findings for next time

- **A release is not finished when it deploys.** `0.6.0` was live and verified
  within minutes, and still had no record a day later. The deploy is the
  reversible part; the record is the part that cannot be reconstructed after the
  terminal scrolls. Write it before moving on, even if it has to say "not
  captured" three times.
- **Do not reason about production's schema from the last release's record.**
  The cut PR said five migrations were pending and one was. Other streams apply
  their own migrations as they merge, so the last record is a snapshot with a
  day of writes on top of it. `--drift production` is cheap and it is the only
  answer.
- **The sign-in lock hides the thing most worth checking.** Every public page was
  redesigned in this release and none of them can be seen signed out, by design.
  Until go-live, verifying public-facing work on production requires a signed-in
  session or the `public_viewer` account added in `0.5.0` — which is exactly what
  that account was built for, and it has not yet been used for it.
