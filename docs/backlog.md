# Backlog

Each item is a self-contained prompt for a new thread. Ordered by suggested priority; reorder freely. When an item is done, tick it and move it to the Completed section at the bottom, under the matching heading, so follow-ups can still be traced back to it.

## Deployment (before go-live)

Environments exist since 2026-09-21 (README "Environments"): `test.lannacare.org` runs `main` against the dev database; `lannacare.org` runs the production Supabase project (`dbkodyyxxhtygxcxmfcu`, all 63 migrations applied, public views checked, Google provider and redirect URLs set, Lutan seeded as admin). Google Drive is the dev account in every environment for now. What still separates this from a live shelter system:

- [ ] **Cut production over to `lannacareforanimals.org` and turn `lannacare.org` into the pre-prod UAT environment.** The customer bought `lannacareforanimals.org` on 2026-09-23 through **sav.com** — it matches the shelter's own Google account (`lannacareforanimals@gmail.com`), so it is also the right home for the shelter's email later. Not urgent, we are not ready to cut over, but it gates go-live, and the Pi origin item below is worth doing *after* it so the tunnel hostnames are created once, on the right zone. Order of work:
  1. **Host the zone on Cloudflare.** It was not registered there, so: add the site in the Cloudflare dashboard on the same account that holds `lannacare.org`, take the two assigned nameservers, set them in sav.com's domain manager, and wait for the zone to go Active (minutes to a couple of days, depending on the old TTLs). Nothing else can start until it is. Recreate the DNS that matters — the `google-site-verification` TXT record if Search Console is re-verified for the new domain (it is needed for the consent screen's brand verification), and Email Routing if the shelter wants `…@lannacareforanimals.org`.
  2. **Stand up a new Supabase project as production** and demote the existing one. Agreed 2026-09-23 and the right way round: the current production project (`dbkodyyxxhtygxcxmfcu`) already holds the real AppSheet migration — realistic volumes and real edge cases, which is exactly what UAT sign-off needs — while the live database should start clean and be loaded at cutover from a fresh export, with the 2026-09-22 import as the rehearsal. New project → `node scripts/apply-migrations.mjs --env production` applies every migration from zero (no baseline needed; dev was baselined at 0027 only because it predated the runner) → Google provider, redirect URLs and SMTP → seed Lutan as admin → re-run `import-appsheet.mjs` against it at cutover. Then `.env.deploy.production` takes the new project's values and the old ones move to `.env.deploy.uat`.
  3. **Decide where the third project lives** — the open cost question. The free tier allows two projects per organisation and this makes three (dev, UAT, production). Recommended: put **production alone in its own organisation on the Pro plan**, which also buys the live system daily backups and PITR — worth having on its own merits, since `scripts/backup.mjs` exists precisely because the free tier has none, and it makes a UAT mistake unable to touch production's quota or settings. The free alternative is a second organisation for production; free projects pause after a week idle, which the live site never would be, but it leaves prod with no managed backups.
  4. **Point the domains.** Move the `lannacare.org` / `www.lannacare.org` custom-domain routes in `wrangler.jsonc`'s `production` environment to `lannacareforanimals.org` and `www.`, give the new `uat` environment the `lannacare.org` route, deploy both, and set the UAT project ref in `src/lib/app-env.ts` in the same change. No application code changes for the hostname itself: `src/lib/site-origin.ts` derives the origin from the request, so copied tag and QR links (`src/lib/tags/origin.ts`, `CopyTagLink`) follow the domain on their own — but the tags and cards already printed carry `lannacare.org`, so either reprint them at the end of the parallel run or leave `lannacare.org` redirecting the tag paths to production.
  5. **Follow the domain everywhere it is registered outside the repo:** Supabase Auth Site URL and redirect URLs on *both* projects; the Google sign-in client's authorised origins and redirect URIs in the `LCA App` Cloud project (endless-bonus-458210-d4), plus the consent screen's homepage, privacy-policy and authorised domains — the app is **published**, so expect a re-review, and both domains need to be listed while UAT still signs in with Google; the Cloudflare WAF rule and `ORIGIN_HOST` for the Pi tunnel; and the tunnel hostnames in `scripts/pi/cloudflared-config.yml` (`pi.lannacare.org`, `test-pi.lannacare.org` and both `httpHostHeader` lines), `scripts/pi/setup.sh` and `scripts/pi/deploy-pi.sh`.
  6. **Split the Drive trees at the same time.** Production moves to a copy of the folder tree in the shelter's own Google account and UAT keeps the existing one on `lannaanimalfoundationBWM` — see the Drive item below, which the cutover should carry out rather than leave for later. Until that is done, UAT and production would share every photo and archive PDF by file ID, and a tester deleting one would delete the customer's copy.
  7. **Treat UAT as holding real personal data**, because it will: adopter and carer names and contact details, not just animals. Same RLS, same role gating, same care with who gets a login as production — a lower environment is not a reason to be looser. Decide too how UAT stays realistic over time: restoring a recent production backup into it before each sign-off round is the obvious answer, and it doubles as the restore rehearsal the item below asks for.
  8. **Then the docs:** README "Environments" and "Deploying to Cloudflare", `docs/pi-hosting.md`, `docs/data-migration.md`, the usage comments at the top of `scripts/deploy.mjs`, and a `docs/decisions.md` entry for why the old production database became UAT rather than the other way round.
- [ ] **Bring the Suphan Buri Pi up as the production origin** (from ~2026-09-28). The Worker is already deployed with the tunnel fallback logic and `ORIGIN_HOST` empty (2026-09-22). Follow `docs/pi-hosting.md` end to end: clone, copy the two env files, create the tunnel, `scripts/pi/setup.sh <tunnel-id>`, the WAF rule and `ORIGIN_KEY`, set `ORIGIN_HOST` and `npm run deploy:prod -- --secrets`, then the failover drill. Do the backup item below on the same box, same day.
- [ ] **Move the weekly backup to the Raspberry Pi 5 at home** (from ~2026-09-28, when Lutan is back there). An always-on Linux box with a 2 TB SSD beats a laptop that has to be awake on Sunday mornings, and the SSD gives a second copy next to Drive. `scripts/backup.mjs` already runs there unchanged: it looks for `pg_dump` on `PATH` first. Steps: install Node 22+ and `postgresql-client-17` from the PGDG apt repo (`apt.postgresql.org` has arm64 builds; Debian bookworm's own package is 15, which pg_dump refuses against a 17 server); `git clone` the repo and copy `.env.local` + `.env.deploy.production` across (never commit them); run `node scripts/backup.mjs --env production` once by hand; then a cron line such as `0 3 * * 0 cd ~/Animal_Shelter_App && node scripts/backup.mjs --env production >> backup.log 2>&1`. Add a `--local-copy <dir>` flag (upload *and* keep a copy on the SSD, unlike `--local`) so the Pi holds the last N too. Then unregister the laptop task (`scripts\backup-schedule.ps1 -Remove`) and update README "Backups". Drive stays the off-site copy — a Pi in the house is one flood or theft away from the data it protects.
- [ ] **Rehearse a restore from a production backup.** `scripts/backup.mjs` runs weekly (2026-09-21, README "Backups") but the restore recipe in the README has never been run. Once, into a scratch Supabase project — the org's free tier allows two, so this waits for a moment when the dev project can be recreated from a restore, or for a third project elsewhere: `pg_restore` the `public` schema, then `auth` data-only, then check `apply-migrations.mjs --status` and sign in. Correct the README recipe with whatever it took.
- [ ] **Finish the AppSheet cutover.** The data is in production as of 2026-09-22 (`docs/data-migration.md`: 0064 applied, `import-appsheet.mjs --env production` loaded 76 residents / 130 placements / 205 attachments, `main` deployed to lannacare.org, `/adopt` and the photo proxy checked). Done 2026-09-22 afternoon: re-exported the sheet and diffed every tab against the 2026-09-21 15:34 UTC snapshot — nothing was entered in AppSheet after it, so nothing to re-enter; all seven `drive-unclaimed` scratch folders are in Drive's trash (`Test Dog Alpha (R-0001)` was hiding under `Deceased/` behind its R-code; `Dang 1 (3c1ac03f)` was *not* empty — it held Dang 1's 2025-12-08 blood-test scan that production's attachment points at, so its `Blood Tests/` was moved into `Dang 1 (R-0043)` first, file ID unchanged); Retry archive — which had never worked on Cloudflare (`docs/decisions.md` 2026-09-22, the PDF-pipeline entry, PR #38) — run on all six deceased residents in production, PDFs and offline indexes verified in Drive. **Done 2026-09-23: the AppSheet app is read-only** — every table set to *Are updates allowed?* → Read-Only in the AppSheet editor. The app was not paused, so it stays readable as reference, but nothing can be entered in it any more — which is what made the parallel run safe: from here the two systems cannot diverge, because only one of them accepts writes. Still by hand: the 1–2 week parallel run from the requirements doc §9 — until about 2026-10-06 — reprinting QR / RFID at its end and retiring the AppSheet app and its Apps Script triggers (`archiveDeceasedResidentFolders`, `onBulkUploadFormSubmit`). Original brief: connect the existing Drive photos. Real records, not the dev scratch data; rehearse against the dev database first (it is disposable), then run against production. Same rehearsal for the Drive folder linking.
- [x] **Publish the production Google sign-in app before go-live.** Done 2026-09-22: consent screen in the Google Cloud project `LCA App` (endless-bonus-458210-d4, Lutan's account — renamed from `My First Project`) is **In production**, so any Google account can reach the Supabase sign-in on `lannacare.org` (Supabase still refuses accounts without a role). Google demanded a privacy-policy URL first — hence `/privacy` (#37, `docs/decisions.md`). Branding: app name `Lanna Care for Animals`, home `https://lannacare.org`, privacy `https://lannacare.org/privacy`, authorised domains `lannacare.org` + the Supabase host; scopes `openid`/`email`/`profile` only, so no verification. Client ID/secret remain in `.env.deploy.production` (`GOOGLE_SIGNIN_*`, not read by the app — kept for re-applying to Supabase). The `1036…` Drive/dev clients live in a project under the dev Google account, not here. Brand-verified the same day so the consent screen says "Lanna Care for Animals" rather than the Supabase host: that needed `lannacare.org` verified in Google Search Console (Domain property, Lutan's account) via a `google-site-verification=…` TXT record on the apex in Cloudflare DNS — **keep that record**.
- [ ] **A 90-day token expiry is set somewhere — expect a breakage around 2026-12-21.** On 2026-09-22 Lutan set an OAuth/API token's validity to the 90-day maximum but couldn't later recall which product; it is *not* in the `LCA App` Google Cloud project (checked: Auth Platform Settings, the sign-in client, the consent screen). Candidates: Supabase (Authentication → Sessions, or a personal access token used by `scripts/apply-migrations.mjs`), a Cloudflare API token used by `scripts/deploy.mjs`/Wrangler, or the Drive client's project under the dev Google account. If sign-in, deploys, migrations or the weekly backup start failing in mid-December, start here; when found, either extend it or note the renewal date on this item and tick it.
- [ ] **Give production its own Drive tree on the shelter's account, and leave UAT on the current one.** Decided 2026-09-23, replacing the earlier "transfer ownership of the existing tree" plan: production gets a **copy** of the tree in the shelter's own Google account (`lannacareforanimals@gmail.com`), and the existing tree stays where it is on `lannaanimalfoundationBWM` for UAT, dev and test. The point is isolation — UAT is where a tester deletes a photo, re-runs an archive, or drops a resident to see what happens, and it must not be able to do any of that to the customer's real files. A copy is what buys that; an ownership *transfer* does not, because the old owner keeps edit access to what is now the customer's data, and removing that access breaks every legacy file ID in the UAT database. It also means the shelter owns its own files, which a developer's personal Drive account holding the only copy of the archive never did. Steps: copy `Residents/`, `Projects/` and the rest of the root into the new account (`files.copy` in a small script, structure and folder *names* preserved — the import matches on them); mint a refresh token for that account (`scripts/google-oauth-setup.mjs`); put the four `GOOGLE_*` values, including the new `GOOGLE_DRIVE_ROOT_FOLDER_ID`, in `.env.deploy.production`, and the current ones in `.env.deploy.uat`; `node scripts/deploy.mjs --env production --secrets`. **No remap script needed** — `scripts/import-appsheet.mjs` resolves attachments by walking the tree under `GOOGLE_DRIVE_ROOT_FOLDER_ID` and *recording* the file IDs it finds, so the cutover import against the copied tree records the copies' IDs by itself. **But rehearse it against the dev database first:** the 2026-09-22 import matched resident folders partly by the AppSheet id in their names, and those folders have since been renamed to R-codes, so the resolution that worked then may not work now. Two smaller things to check: the shelter's free 15 GB has to hold the tree and the archive PDFs it will keep generating, and the `1036…` Drive OAuth client lives in a project under the dev Google account — the new refresh token can keep using it, or it moves alongside.
- [x] **Configure SMTP in the production Supabase project before go-live** Done 2026-09-23 for the current project (`dbkodyyxxhtygxcxmfcu`, which becomes UAT): Resend free, `lannacare.org` verified, sender `Lanna Care UAT <noreply@lannacare.org>`, and a real Forgot password mail was received. The new production project at the cutover needs the same on `lannacareforanimals.org` (cutover item step 2; `docs/email-sending.md` last section). (redirect URLs are done). Forgot password (done 2026-09-21) sends through Supabase Auth, whose built-in mailer is development-only. **Provider decided 2026-09-23: Resend free tier** (Lutan; must cost nothing). Lutan creates the account, adds the DNS on `send.lannacare.org` and fills Supabase SMTP. Every field is in `docs/email-sending.md` §2 (lands with PR #62). Resend free holds one domain, so Production on `lannacareforanimals.org` needs a second free account or the paid plan; decide at the cutover.
- [x] **Dev and Test look different from production.** Done 2026-09-22: anything built against the dev Supabase project is teal with a green-cast background and a **Dev** badge in the header; production keeps the orange (`src/lib/app-env.ts`, README "Environments", `docs/decisions.md`).

## Quick wins

- [x] **Move the "Public website" link off the left nav and onto the header logo.** Done 2026-09-22: the roundel + wordmark in `AppHeader` is one `<Link href="/">` that opens in a new tab (`target="_blank"`, `rel="noopener"`), titled and labelled `t.nav.publicSite`; the nav entry is gone and the manual's navigation topic says so. Original brief: Customer request 2026-09-22. `src/app/NavLinks.tsx` lists `{ href: "/", label: t.nav.publicSite }` as an ordinary nav entry; the logo roundel + "LCA" wordmark in `src/app/AppHeader.tsx` is plain markup that links nowhere. Make the logo and wordmark one `<Link href="/">` (with `title`/`aria-label` from `t.nav.publicSite` so it is discoverable and screen-reader friendly), drop the nav entry, and check the phone drawer still has nothing referring to it. Consider opening the public site in a new tab (`target="_blank"`) since staff are mid-task when they click it; decide with the user. Update the manual page that mentions the Public website link if there is one (`src/lib/manual/`).
- [x] **Accordion the Management and Admin nav sections, collapsed by default.** Done 2026-09-22: a chevron button (`aria-expanded` / `aria-controls`) beside the parent link toggles the group; the label still navigates to the landing page. Collapsed on load, auto-opens whenever the route is inside the group, and the user's choice is kept in `sessionStorage` (`nav-expanded`); sidebar and drawer share the state. Original brief: Customer request 2026-09-22. In `src/app/NavLinks.tsx` the two parent items with `children` (Management: dashboard, contacts, vets, medications, diets, translations; Admin: website, security, enclosures, zones, immunization/procedure/blood-test types) always render their children expanded, so an admin sees ~15 links before the fold. Make each parent a toggle (chevron, `aria-expanded`) that starts collapsed on app load, auto-expands when the current route is inside it (so a refresh on `/admin/zones` still shows where you are), and remembers the user's choice for the session (`sessionStorage`, wrapped in try/catch). Clicking the parent label should still navigate to its landing page (`/management`, `/admin`) — put the toggle on the chevron, or expand on first click and navigate on the second; decide with the user. Same behaviour in the phone drawer. Non-admin roles are unaffected (they never see these groups).
- [ ] **Walk through the 2026-09-21 offline session's changes with the user.** Twelve features merged from a phone-driven session (PRs #16–#27) with type-check, lint, build and scripted database checks but no click-through of the UI, so the review is the first time a person sees them. Do it as a guided tour in the running app, one screen at a time, in this order — each stop says what to look at and what decision it embodies:
  1. **Prescriptions** (#16) — a resident's Prescriptions tab: Edit link on every row, End today on current rows that have started (withheld from a future-dated course). `/prescriptions/[id]/edit`.
  2. **Blood test types** (#17) — Admin → Blood Test Types (seven seeded panels, add/rename/merge); the blood test form's required Test type dropdown defaulting to CBC; the type on the Blood Tests tab. *Decision to confirm:* every pre-existing test was backfilled to CBC rather than "Unknown" (`docs/decisions.md`).
  3. **Dietary requirements + resident size** (#18) — intake form's required Size and optional Starting diet; the edit form demands a size on first save for older residents; hub Diet card and Diet tab (Current / Past, Add / Edit / End today); Management → Diets (unit, ฿/unit, S/M/L daily quantities, 7/30-day forecast with cost and a total); size on `/adopt`. *To do during review:* add the first real diet types with prices and portions, since the list starts empty. *Decisions:* per-size quantity lives on the diet type with a per-resident override; fostered residents excluded from the forecast; unset size counts as Medium.
  4. **Deceased editable fields** (#19) — open a deceased resident: the edit page now shows only Profile photo and Bio & background; the Photos tab keeps its uploader; after a change the archive PDF / index in Drive are regenerated. Everything else still locked (try to confirm the pencil is back but identity is not offered).
  5. **Forecast window picker** (#20) — Management → Medications and → Diets: From/To above the table adds a third column; Clear removes it; the range is in the URL.
  6. **Maintenance** (#21, corrected by #26) — log/edit form's Assigned to lists **logins** (staff, volunteer, management, admin — not contacts); assignee on the job page and board cards; **Delete job** on the job page removes photos and the Drive folder. *Decision:* jobs are assigned to logins because the person actioning is never a carer or supplier; the contact hub's jobs list is gone accordingly.
  7. **Vet visits** (#22) — Edit on each row of a resident's Vet Appointments tab (the first way to change a visit after booking): status incl. Cancelled, date, vet, reason, notes, **cost**. Vet hub: Spend card for the period, cost per row, and the vet's notes; Management → Vets has the notes field.
  8. **Contacts** (#23) — notes on the Management → Contacts form/table, on the contact page, searchable from the list.
  9. **Passwords** (#24) — Admin → Security: create a test user and see the temporary password shown once with Copy; sign in with it in a private window and confirm you're held on Choose your password until one is set, then land on Residents; Issue temporary password on a row; Change password in the menu. *Decision:* the flag is in `app_metadata`, not `user_metadata`, because a user can edit the latter (`docs/decisions.md`).
  10. **Access requests** (#25) — Admin → Security: sign in with a Google account that has no role (or look at any "No role" leftovers) and see it under Access requests with Approve (role) / Deny; the users table shows only accounts with a role.
  11. **Forgot password** (#27) — Forgot password? on the login page → email → the link from the dev mailer → Choose a new password → signed in. *Production prerequisite:* SMTP + redirect URL in Supabase (Deployment checklist; README step 5).
  12. **Housekeeping to confirm together:** migrations 0050–0055 are applied to dev and pending for production; `docs/decisions.md` has six new entries dated 2026-09-21; new backlog items split out today: stock on hand / days-of-stock, legacy AppSheet maintenance folder migration, how carers share LINE/Messenger, SMTP before go-live. Tick this item once the tour is done and log anything found as its own backlog item.
## Mobile

- [ ] **Review which admin features (if any) belong on mobile.** Intent: the phone experience stays a field tool — what a volunteer or staff member needs while standing in an enclosure (find a resident, log weight/immunization/vet visit/procedure, move or hospitalise, add photos, check enclosure occupancy). Admin-type work (Admin → Security, Website, Zones, Enclosures, Immunization types, future Vets / Contacts / Projects / Medications admin pages, and any bulk or reporting screens) most likely lives on desktop only, but decide that per screen rather than by assumption. Go through every route under `/admin` plus the desktop-only affordances elsewhere (residents table bulk selection, zone/enclosure filters, edit forms with many fields) and for each record: needed in the field / nice-to-have / desktop only, with a one-line reason. Output is a short table in `docs/decisions.md` and the resulting changes: hide desktop-only nav entries and pages below the `md` breakpoint (or show a "use a larger screen" notice rather than a broken layout), and make sure anything marked field-needed is covered by the Mobile responsiveness sweep below. Also check the role angle — volunteers never see admin pages anyway, so the question is mainly what admins/staff reach for on a phone.
- [x] **Inline-edited rows show stale data until reload.** *Closed 2026-09-24 as not reproducible on Next 16.3.5: with `refresh()` removed, button-driven rename and delete updated the row in place on zones and procedure-types (the page it was reported on). No code changed; see decisions.md, 2026-09-24.* Found 2026-09-21 while building `/admin/procedure-types`: a server action called from a button (`startTransition(() => updateX(...))`, as every admin/management table's Save / Delete / Merge does) revalidates the path but does *not* refresh the client router on Next 16, so the row keeps showing the old name until the next navigation — only `<form action>` submissions refresh on their own. The fix is `refresh()` from `next/cache` after the `revalidatePath` calls in the action (procedure-types does this; see its `revalidateProcedureTypePages`). Apply the same one-liner to the immunization-types, zones, enclosures, security, contacts, vets and medications actions.
- [ ] **Mobile responsiveness sweep.** Go through every screen (residents list, resident hub, forms, admin pages, login) at phone width and fix layout, table overflow, and spacing so it feels professional in portrait.

## Resident operations

- [ ] **Deceased residents: confirm what happens to the archive when photos or the bio change after death.** Customer question 2026-09-22 — check the business logic and close the gaps. *How it works today:* the three edits still allowed on a deceased resident (0052: bio via `/residents/[id]/edit`, photo upload via `src/app/api/residents/[id]/photos/route.ts`, set-profile-photo and delete-photo in `src/app/residents/[id]/photos/actions.ts`) each call `refreshDeceasedArchiveIfNeeded()` (`src/lib/archive/refresh-deceased-archive.ts`), which re-runs `archiveDeceasedResident()` — the folder move no-ops and the summary PDF and `index.html` in `Residents/Deceased/<Name> (<ID>)/` are replaced in place, so the archive follows the record. *Gaps to decide on:* (1) it is **best effort and silent** — every caller ignores the returned `{ error }`, so if Drive is down the edit saves but the PDF/index stay stale and nobody is told; either surface it (hub banner "Archive out of date — Retry", reusing the existing Retry button) or record a `deceased_archive_stale_at` on `residents` that the hub shows. (2) It only runs when `deceased_archived_at` is set: a resident whose first archive never completed does not get one from an edit — fine, but the hub should say so. (3) A multi-photo upload regenerates the PDF once **per file** (sequential uploads); cheap for a handful, but debounce or run once after the batch if it turns out slow. (4) A HEIC profile photo is never embedded in the PDF (`EMBEDDABLE_IMAGE_TYPES`), so choosing a phone photo as profile after death gives a PDF without a picture — convert on upload or note it in the manual. (5) Verify in dev end to end: mark a test resident deceased, add a photo, change the bio, set a different profile photo, delete a photo — open the PDF and `index.html` in Drive after each step and confirm the change is there; write the outcome into the manual's deceased topic.
- [ ] **Regenerate the six production deceased-resident summary PDFs once the layout fix is live.** Filed 2026-09-24 from `claude/deceased-pdf`. Every archive PDF generated before that fix has the resident's name printed over the subtitle and no footer (name, ID, generated date, page N / M), and a deploy does not rewrite them — only a bio/photo edit on the deceased resident, or the hub's Retry for an archive that never completed, calls `archiveDeceasedResident()`. Six residents in production have one. Release-time work, after the deploy that carries the fix: regenerate each (re-saving the bio unchanged should do it via `refreshDeceasedArchiveIfNeeded()` — confirm that a no-op save actually triggers it, or add an admin "Regenerate archive" action if not) and open each PDF in Drive to check.
- [ ] **Foster carer portal (future).** New RBAC role with access scoped to the residents currently fostered with that carer (`resident_current_state.current_carer_id`), allowing them to log weight, immunizations, vet visits and appointments. Needs a `user_id` on `contacts` (or a join table) to link a login to a carer, plus RLS policies keyed off it.

## Medical records

- [ ] **Stock on hand and days-of-stock.** Neither medications nor diets record what's actually in the cupboard, so the forecasts say what will be used but not when it runs out. If the shelter wants that: a `stock_on_hand` (and maybe `reorder_at`) figure on `medication` and `diet_types`, edited in place on the two management pages, and a "days of stock" column computed from the 30-day rate — with a flag when it's inside the reorder lead time. Split out of the medication requirement page item when that closed (2026-09-21).

## Facility

- [ ] **A public view behind enclosure QR codes.** Raised 2026-09-22 when the tag links landed (#41, `docs/decisions.md`): a resident's card (`/r/<code>`) now shows a visitor a public card and a signed-in user the hub, but an enclosure's QR code (`/e/<id>`) still sends a visitor to sign-in, because there is no public enclosure page. If visitors are expected to scan the codes on the kennels — a walk round the shelter reading who lives where — give `/e/<id>` the same shape as `/r/`: signed in → `/enclosures/<id>`; signed out → a public page with the enclosure's name (and Thai name), zone, and the residents in it as cards linking to their `/r/` pages. Needs a schema PR first: an anon-readable view (say `public_enclosure_residents`: enclosure id, names, zone, and per resident the card fields from `public_resident_cards` 0068) — occupancy against capacity, notes and maintenance stay staff-only, and the Lifecycle pseudo-enclosures (Hospital, Fostered…) must not appear. Then `/e/` joins `PUBLIC_PATH_PREFIXES` / `isPublicPage` in `src/lib/public-paths.ts`, `check-public-views.mjs` gets the view, and the manual's enclosure-page topic says what a visitor sees. Decide with the user whether an enclosure page should show the residents at all, or only the enclosure — a visitor who scans a kennel is looking at the animals in it, so residents is the useful answer, but it is the shelter's call.
- [x] **Filter the enclosures page to those with open maintenance.** Done 2026-09-24: `?maint=open`, a Has open maintenance tick beside search and sort; counts jobs on the enclosure only, not zone-wide ones (`docs/decisions.md`); hides the Lifecycle cards; hidden and ignored for vets. Raised 2026-09-24: a "Has open maintenance" toggle on `/enclosures` (beside the zone filter and search in `EnclosureFilters`, kept in the URL like `zone`/`q`, e.g. `?maint=open`) that shows only enclosures with a job not `Completed`. No migration: `src/app/enclosures/page.tsx` already loads open jobs per enclosure into `open_jobs`, so it is one more `.filter()` plus the control, an i18n string (en + th) and a line in the manual topic. Decide while building: zone-wide jobs (`enclosure_id` null, counted in `zoneWideJobs`) — either count them for every enclosure in that zone or leave them out and say so in the manual; and hide the pinned Lifecycle cards (Hospital / Unassigned / Fostered) when the filter is on. Vets cannot read `maintenance` (RLS), so for them the filter would always be empty — hide it for the vet role.

## Admin

- [ ] **User preferences: pick a colour theme.** Requested 2026-09-23 by Lutan. Let each user choose the app's look and feel from a small set of colour themes (e.g. the current dark orange, a light theme, a high-contrast one, and one or two alternative accents), saved per user so it follows them between devices — a `user_preferences` row or `user_metadata` (preferences, not permissions, so user-editable metadata is fine here), with `localStorage` only as a no-flash cache. Put the picker in the account menu beside Change password. The groundwork is there: every component reads CSS tokens from `src/app/globals.css`, so a theme is a `[data-theme="…"]` block redefining `--background`, `--surface`, `--primary` etc., set on `<html>` by the layout. **The catch is the environment colouring:** `:root[data-env="dev"]` recolours the same tokens teal so dev is never mistaken for production, and a user theme must not erase that signal — decide whether the env marker moves to something themes don't touch (a header strip or badge, as the `uat` item plans) or each theme gets a dev variant. Check contrast (WCAG AA) for every theme, and the generated PDFs stay unthemed.
- [ ] **How carers share LINE and Messenger.** The `/contacts` list shows LINE IDs but LINE's `ti/p/~` links only resolve IDs the person has made searchable; confirm with staff how carers usually share LINE (ID vs. QR / `line.me` link) and store whichever works. Same question for Messenger (username vs. a shared `m.me` link) — both are accepted as-is when pasted. Was (b) of the Contacts follow-ups item; (a) notes and (c) the maintenance link closed 2026-09-21.

## Management

- [ ] **Base the vet forecast on recent visit frequency, not a flat per-visit estimate.** Asked for by Lutan 2026-09-23 while testing PR #60: "can we set it to look at an average rolling number of visits recorded per week in the last quarter x the cost per vet visit, or number booked for that week if it's more." Today the vet line is a flat typical-visit estimate edited on `/admin/website` — it does not know how often the shelter actually goes. The proposed rule is, per week, `max(rolling average visits/week over the last quarter, visits actually booked that week) x cost per visit`, so a quiet week still carries the historical rate and a busy one is not understated. Decisions to make before building: (a) the page forecasts by **month**, so either the weekly figure is aggregated up or the rule is restated monthly; (b) whether "cost per vet visit" stays the flat estimate or becomes the mean of actual `vet_appointments.cost` over the same quarter, which would make the whole line self-maintaining; (c) what a booked visit with a **known** cost does — it should surely use its own cost rather than the estimate, which means the rule is really `max(estimated visits, booked visits)` for the *count* and actual cost where known; (d) the empty case, where a shelter with no history gets an average of zero and the line silently reads nothing rather than "unknown". Note this interacts with the defect PR #60 already fixed, where `coalesce(va.cost, estimate)` over a LEFT JOIN charged the estimate to months with no visits at all — any frequency-based rule needs the same care about distinguishing "no visits" from "no data". **Looked at 2026-09-24 and left as the flat figure on Lutan's call:** dev held 71 completed visits, none with a `cost`, and 20 in the last quarter (about 1.5 a week) — so the frequency half has data but the cost half does not, and the change needs a migration while 0073 was in flight. See `docs/decisions.md` (2026-09-24). The dev query and the proposed rule are recorded there, so this is ready to pick up once invoices are being recorded.


- [x] **Archive contacts instead of deleting them.** Done 2026-09-24: schema in 0075 (#86); Archive / Restore on `/management/contacts` and `/contacts/[id]`, Show archived toggles on both lists, search still finds archived contacts, archived carers left out of `loadCarerOptions` and refused by `rehomeResident`. Customer request 2026-09-22. A carer who no longer fosters or adopts, a volunteer who has moved on, a supplier the shelter stopped buying from — none of them can be removed today: `deleteContact` in `src/app/management/contacts/actions.ts` refuses when `placement_history.carer_id` references them (rightly, that history must survive), and deleting the rest loses notes and contact details someone may need again. Add `archived_at timestamptz` (+ `archived_by`, optional `archive_reason` text) to `contacts` — a new migration, nullable, no RLS change. **Behaviour:** an Archive action (with reason prompt) on the contact row in `/management/contacts` and on `/contacts/[id]`; archived contacts drop out of the default `/management/contacts` and `/contacts` lists and out of every picker (`loadCarerOptions` in `src/lib/contacts/carers.ts`, the vendor/donor lookups) but stay reachable through a "Show archived" toggle, shown muted with an Archived badge and the reason, and can be Restored. **Where they still appear:** a resident's housing history keeps naming an archived carer (with the badge), and the foster/adopt form refuses to place a new animal with one unless the contact is restored first — that is the point of archiving. Keep `deleteContact` for contacts with no history at all (a typo entry); everything else archives. Reuse the same pattern the deceased-resident and hidden-deceased-list items use for "default hidden, toggle to show", and add `archived` to the contact search so a name lookup still finds them with the badge rather than reporting no match.
- [ ] **Contacts: who should see a carer's phone and address?** *Waiting on the director* — deferred to them 2026-09-21; don't start until they've said which roles need which fields. Raised 2026-09-21 in the first staff walk-through: `/contacts` (list and hub, with phone, LINE, address and one-tap call / maps) is open to every signed-in role, and the RLS in 0001 gives staff read/write and volunteers/vets read on `contacts`. The concern is privacy — a volunteer shouldn't be able to look up a carer's home address. The counter-case, recorded in `src/app/contacts/page.tsx`, is that a volunteer doing a foster pick-up needs the carer's number. Options: (a) move the directory under Management (nav + `requireManagementUser()` on `/contacts` and `/contacts/[id]`) and leave the RLS alone so the carer pickers on intake / rehome keep working for staff — quick, but staff then have no way to phone a carer; (b) keep `/contacts` for staff and management, hide it from volunteers and vets, and drop the address from the list for everyone but management; (c) tighten RLS with a `contacts_directory` view that omits phone/address for non-management, which also protects the data from anyone poking the API. Decide with the user which roles need which fields, then do it in one pass with the manual and the README roles table.

- [ ] **Machine-drafted translations (phase 2 of the free-text strategy).** The plumbing from 2026-09-21 (`0056_translations.sql`, `/management/translations`, `TranslationPanel`) has a human writing every draft; this adds a worker that fills `translations.text` with `status = 'draft'` so the managers check rather than write. Constraints agreed with the user: zero ongoing cost, open-weight models only, no PC on site. Shape: a Cloudflare Worker on a cron trigger in the same account the app deploys to (Workers AI binding, so no API key; Llama 3.x / Qwen / M2M100 all cover Thai and the free daily allowance dwarfs shelter volume), polling `translations where status = 'pending'` through Supabase REST with the service key as a Wrangler secret, writing back `text`, `engine` and `status = 'draft'`; a translate() function with one implementation per engine so a local Ollama box can replace it if one appears. Prompts differ by tier ("translate this adoption bio naturally; keep breed names" vs "translate this clinical note literally; keep drug names, doses, units and dates as written"). Then add the internal tier to `translatable_fields` (weight / vet-visit / prescription / intake notes, cause of death, behaviour notes) and attach the queueing trigger to those tables — shown to staff as a labelled machine translation under the original with **no** review step (the Weight tab finding from 2026-09-20). Before building, run twenty real bios and twenty real vet notes through two candidate models and let the two bilingual managers pick. Backfill in batches so the managers get ~20 Tier A drafts a day, not three hundred on Monday.

- [ ] **Salaries: pay the people, on the 15th and the last day of the month.** Customer request 2026-09-23 (the Director). The system models animals, not the people paid to look after them. Someone on the payroll exists today only as a login — `auth.users` + `user_roles`, surfaced through the `app_users` view (0055, archivable via `archived_at` in 0063) — or, if they never sign in, nowhere at all; `contacts` (0001) covers carers, volunteers and suppliers but holds no employment terms, and `contact_type` dropped `Donor` as unused in 0067. So there is no employee record, no pay rate and no payment history anywhere in the database. The ask is all three of the things the shelter actually pays out: **standard salary paid twice a month — on the 15th and on the last day of the month — plus overtime, plus reimbursed expenses.** **Ask the Director before any schema:** (a) who is on the payroll and how many, and whether pay is a fixed monthly salary split into two equal payments or a daily rate × days worked — that answer decides whether a period's base amount is derived or typed in; (b) how overtime is agreed and at what rate — don't assume the Thai labour-law multipliers (1.5× on a normal working day, 3× on a holiday), record what they actually pay; (c) what counts as an expense — petrol for a vet run, feed bought in town, a phone top-up — and whether a receipt has to be kept with it; (d) whether anything is deducted (social security, withholding tax), which is the fork between a **payment log** (what left the shelter's hands, which is all the Cashflow item above needs) and a **payroll system** (gross, deductions, net, payslips, filings) — build the payment log unless they say otherwise, and say on the page that it is one; (e) who may see the figures, and whether a person should be able to see their own — that last one is a schema decision rather than a preference, because "their own" turns the policy from a role check into a role-or-self check keyed on `employees.user_id`, and that column stops being an optional convenience the moment access depends on it; note too that anyone paid who has no login could then never see their own row, which is a perfectly fine answer but should be a chosen one. It is far easier to design a role-or-self policy in than to retrofit one. **Privacy is the constraint that shapes the rest, and it has to be enforced by something that runs, not by care at review time.** This would be the most sensitive data in the app. Its own tables with their own policies, admin + management only, following `0039_management_access.sql` — and explicitly **not** `site_content`: `0018_site_content.sql:50` is `create policy public_read_site_content ... for select using (true)`, an unqualified SELECT, so every column of that table is readable by anyone holding the anon key, which ships in the client bundle by design. `SITE_CONTENT_COLUMNS` in `src/lib/site/content.ts` governs only what the app's own queries select; it is hygiene, not a boundary, and PostgREST will serve a column it omits. The one thing protecting `site_content` today is that nothing in it is sensitive. Note also that `scripts/check-public-views.mjs` is **not** the safety net it looks like — it is a hardcoded allowlist of four views, checked readable-but-not-writable by anon; it never enumerates what anon can reach, so it would not flag a sensitive column added to a world-readable table nor a new table given a permissive policy by accident. So this item owes a check of its own, run in CI beside the other three: with the anon key *and* with a volunteer/staff token, assert that the salary tables answer with permission denied, and assert via `pg_policies` that no policy on them names `staff`, `vet` or `volunteer` — the inverse of the policy mirror 0039 already walks. Beyond the data itself: the page guard is the part that rots, so put it in one shared place rather than inline in each server component (RLS protects the figures; a forgotten guard still leaks the employee names and headcount), and the assistant (`assistant_actions`, 0070) is the sleeper — it composes queries it was not specifically designed for and runs as whoever is signed in, so denylist these tables there explicitly and test it rather than assuming RLS covers it. No salary figure on the management dashboard, where someone is reading over a shoulder. **Schema first, its own PR** (additive, nothing existing changes behaviour; `claude/cashflow-schema` holds 0071, so this takes 0072 once that merges — only one in-flight branch may carry a migration, and if any of this adds an enum value it is two numbered files, not one): `employees` — name, optional `user_id` and optional `contact_id` (not everyone paid has a login, so the row must stand on its own), job title, start date, `monthly_salary numeric(12,2)`, nullable `overtime_hourly_rate numeric(12,2)`, and `archived_at` following the 0063 pattern rather than deleting anyone; then a ledger rather than three parallel tables — `pay_periods` (`period_start`, `period_end`, `pay_date`, `paid_at`, `paid_by`) and `pay_items` (`employee_id`, `period_id`, `kind` ∈ `salary` / `overtime` / `expense` / `adjustment`, `amount numeric(12,2)`, nullable `hours numeric(6,2)` for overtime, note, `recorded_by`). Typed rows mean a bonus or a deduction later is one more enum value, not another table, and a period total is a single `sum`. Baht as `numeric(12,2)` like `vet_appointments.cost` (0053) and `maintenance.estimated_cost` (0033), displayed through `formatBaht` in `src/lib/format.ts`. **The two dates are the fiddly part.** The second period ends on the 28th, 29th, 30th or 31st depending on the month, so compute it (`date_trunc('month', d) + interval '1 month - 1 day'`) and never store "31". Generate the periods from a date range instead of having a cron create rows — no scheduler to babysit and nothing to drift — and write the period row the first time someone opens or confirms it. The month-boundary time-zone problem logged under Dashboard follow-ups (e) lands exactly here: a payment dated the 15th must not read as the 14th, so use the shelter time-zone constant once it exists. **Interface:** `/management/salaries`, management + admin only, nav entry under Management. Open on the current period — its two dates, its pay date, the total, and one line per employee (base + overtime + expenses) with overtime (hours or an agreed amount) and expenses added inline; a "mark as paid" that stamps `paid_at` and freezes the period, with an undo. Past periods newest-first with their totals, and a per-employee view — what this person has been paid this year — because that is the question that actually gets asked. A print or copy-as-text view for whoever hands over the cash and keys in the transfers, same idea as Dashboard follow-up (a). Phone matters: this gets checked standing up in the office, so the period lines must read on a narrow screen. **Design it to meet the Cashflow forecast item above,** which excludes salaries today only because nothing in the database holds them. Once `employees.monthly_salary` exists, salary is the largest and most predictable outgoing the shelter has: add a `salary` category to `cashflow_forecast` covering the contracted pay for the periods falling in the window (`basis = 'priced'`), and leave overtime and expenses out of the forecast rather than guessing at them. Whichever of the two items ships second does the joining, and the "no salaries" sentence comes off the Cashflow page then. **Follow-ups worth noting, not building:** a payslip to hand the person (bilingual PDF); Thai social security and withholding tax, which is a real obligation and a far bigger item than this one; leave and attendance tracking, which overtime hours will eventually want; a bank transfer file; per-employee year totals for the annual report. Manual topic under Management, dictionary strings in both languages, and a `docs/decisions.md` entry recording the payment-log-not-payroll decision and who is allowed to see the figures.
- [ ] **Dashboard follow-ups.** The first cut is deliberately rough; refine with the user once they've seen it against real data. Candidates: (a) **Export / print** — a "copy as text" or print stylesheet so the month section can go straight into the monthly report or a LINE message. (b) **Year view** — the same sections for a calendar year, and year-over-year comparison. (c) **Blood work location** — the report distinguishes "MW" from "vet visit" blood work; the dashboard splits on whether a test is linked to a vet appointment. If "MW" means a specific clinic rather than in-house, add a location/clinic field to `blood_tests` (or link every test to a visit) and split on that instead. (d) **Vet costs** — depends on the cost column in the Vet hub follow-ups item; spend per month belongs here. (e) **Time zone** — month boundaries for timestamp columns (`placement_history`, `vet_appointments`) are computed in the server's time zone (UTC on Cloudflare); a placement recorded before 07:00 on the 1st Thai time lands in the previous month. Decide on a shelter time zone constant and use it here and in the vet hub. (f) **Immunizations** — doses given this month and the stock forecast (decisions.md, open item 6) are the obvious next cards. (g) **Public "our impact"** — `public_shelter_stats` could grow lifetime totals (adopted since founding, sterilisations from project data) once the figures are trusted.
- [ ] **Move frequency options to Admin, apart from medication management.** Raised 2026-09-21: `/management/medications` currently carries two things — the medication list with its 7/30-day forecast (a manager's concern) and the "Frequency options" pick-list (label + schedule: times a day / every N days-weeks-months / as needed, 0044). The frequency list is system configuration, like immunization types and procedure types, and belongs under Admin. Split it out to `/admin/frequencies` (same shape as `/admin/immunization-types`; move `CreateFrequencyForm` / `FrequenciesTable` and the frequency actions, keep the shared `FrequencyScheduleFields` + `parseSchedule`), add it to the Admin nav, and leave `/management/medications` as medications only. RLS: `management_rw_frequency` (0043) would no longer be needed — decide whether to revoke it back to staff's read + insert twin, or leave management able to add one inline from the prescription form. The procedure-types page (`/admin/procedure-types`, done 2026-09-21) is the template — one page per list, same table shape.
- [ ] **Cashflow follow-ups.** Raised 2026-09-23 while building the forecast page, none of them blocking. (a) **Forecast against actuals** — once a few months have passed, show what a month was forecast to cost beside what it did, which is the only way to find out whether the flat vet estimate and the diet quantities are anywhere near right. (b) **A per-vet or per-reason vet average** to replace the single flat `site_content.vet_visit_estimate`, once `vet_appointments.cost` has enough invoice history to mean anything (see `docs/decisions.md`, 2026-09-23). (c) ~~**CSV export** for the monthly report~~ — done 2026-09-24: Download CSV beside the table, with a "not priced" count column per category so an exported 0 never hides a gap. (d) **The fixed windows have diverged:** Diets and Medications draw 7 and 30-day columns from `FIXED_FORECAST_DAYS` in `src/lib/management/forecast-window.ts`; Cashflow shows one window at a time and carries its own `CASHFLOW_FIXED_DAYS` of 30 and 90, because 7 days of outgoings is not a useful figure and changing the shared constant would have reshaped the other two pages mid-flight. Decide whether that is the permanent answer or whether the three pages should share one set. (e) **Immunization doses are counted once per resident per type per window** — `immunization_next_due` (0007) gives the next due date only, so a 90-day window does not see a second monthly dose. It understates rather than overstates, which is the safe direction, but a window longer than the shortest interval is wrong by construction and worth fixing if anyone forecasts a year. (f) **Salaries** are excluded only because nothing holds them; the Salaries item above says to add a `salary` category to `cashflow_forecast` and take the "no salaries" sentence off the page when it lands.

## Assistant

- [x] **Assistant demo: show it to the Director and record what they said.** The sketch is built and merged (2026-09-22): `/assistant` in the left nav with a *demo* badge, admin/management/staff, keyword parser in `src/lib/assistant/demo-parser.ts`, writes via the real move helper and vet-visit RPC after a preview card — see `docs/decisions.md` (2026-09-22, "hard-codes the understanding, not the doing") for what was deliberately left out of the sketch (slide-over, `dryRun`, env flag, audit table). **What remains:** walk the Director through "move Panda to B1" and "book a vet visit for Panda tomorrow 10am", then record what they said in `docs/decisions.md` and let that steer the full assistant item below. Original brief, kept for the real build — raised 2026-09-22: No LLM, no API key: a chat panel whose "brain" is a small keyword matcher that recognises two requests and walks each through the real flow — (1) "move <resident> to <enclosure>" and (2) "book a vet visit for <resident>" — everything else gets a polite "I can only help with moves and vet visits in this demo." **Build the reusable parts properly, hard-code only the brain:** `src/components/assistant/AssistantPanel.tsx` (slide-over from a header button, message list, input, typing indicator, works on the phone), `src/lib/assistant/types.ts` (the message / tool-call / request-details / confirm-card shapes the real assistant will use), `PreviewCard` ("Move Panda (R-0042) from A3 → B1 today" with Confirm / Change / Cancel) and `RequestDetailsModal` (a small form built from a field spec — used when the resident name is ambiguous or the enclosure / vet / date is missing). The scripted brain lives in `src/lib/assistant/demo-brain.ts` behind the same `respond(messages) → AssistantTurn` interface the LLM adapter will implement later, so swapping it out is one import. **It should do real work:** resident and enclosure lookups hit `resident_list_view` / enclosures for real (so the Director can try it on Panda), the move preview calls `src/lib/placements/move.ts` with `dryRun` and, on Confirm, for real; the vet visit preview pre-fills date / vet / reason and on Confirm inserts the `vet_appointments` row the same way `/vet-visits/new` does. Both paths reuse the existing capacity warning and the existing confirm wording, and the resulting rows carry a "via assistant (demo)" note. **Guard rails:** behind a `NEXT_PUBLIC_ASSISTANT_DEMO=1` env flag so it is on for the customer showing and off otherwise; admin/management/staff only; English and Thai trigger phrases (a handful of each — the matcher is a list of regexes, not NLP). **Not in scope:** anything the two scripts don't cover, memory between messages, streaming. Afterwards, record what the customer said in `docs/decisions.md` and let that steer the full item.
- [ ] **Assistant v1: grow the keyword assistant into the production tool until an LLM takes over.** Customer direction 2026-09-22: the Director loved the demo, so the scripted assistant becomes version 1 in production, and the LLM item below becomes version 2 — same panel, same preview cards, a smarter brain later. First, close the demo item above: write the Director's reaction into `docs/decisions.md` and tick it. **Promote, don't rewrite:** keep `src/lib/assistant/demo-parser.ts`'s shape (a request → a `Draft` with nulls for whatever the text didn't say → an editable preview card → Confirm runs the app's own helper) but split it into one small parser per intent behind a `parse(text, context) → Draft | null` list tried in order, so each new function is a file, not a bigger regex. Drop the *demo* badge from `NavLinks.tsx` and `page.tsx` once at least the first batch below is in. **Functions to add, in this order** (each reuses an existing server action or RPC under the caller's session — never a new write path — and stamps notes with `via the assistant — "<request>"` as the demo does): (1) **hospital**: "send Panda to the vet hospital" / "Panda is back from hospital" → `sendToHospital` and the return path in `src/app/residents/[id]/hospital/actions.ts`; (2) **weight**: "Panda weighs 12.4 kg" / "log weight 12.4 for Panda" → the action in `src/app/weight/new/actions.ts`; (3) **death**: "Bella died last night, lung problems" → `recordDeath` in `residents/[id]/deceased/actions.ts` with the `ConfirmDialog` wording the deceased page uses, date left blank when "last night" straddles midnight; (4) **foster / adopt / return**: "Panda goes to foster with Khun Noi" / "Panda has been adopted by …" / "Panda came back from foster" → `rehome` in `residents/[id]/rehome/actions.ts`, carer matched against the same option list the form uses (`loadCarerOptions`); (5) **immunization**: "Panda had rabies today" → the `/immunizations/new` path; (6) **read-only lookups** that need no confirm: "where is Panda", "who is in B1", "what is due this week" — answered inline from `resident_list_view`, the enclosure page's query and the due-soon queries the dashboard already runs. **Thai** trigger phrases for every intent alongside English, as the demo has for move and vet. **Ambiguity:** when a name matches more than one resident the card offers the candidates with code, enclosure and photo — the demo's "pick one" count becomes a real picker. **Where it lives:** keep `/assistant` as the full page and add the slide-over `AssistantPanel` from a header button so it can be used from any screen, including the phone; the message list and input components should be the ones the LLM version will keep. **Roles:** admin / management / staff can write; volunteers get the lookups only. **Audit:** now that it is production, add the `assistant_actions` table from the LLM item (who, when, request text, intent, draft, result) in its own schema PR first — it is the eval corpus the LLM version will be measured against, so start collecting from day one. **Manual:** a new topic in `src/lib/manual/en.ts` / `th.ts` listing exactly what the assistant understands, phrased as the sentences to type — for a keyword assistant the manual *is* the grammar. Ship in batches (hospital + weight + lookups first, then death + rehome, then immunization) so each is a small PR and the Director sees progress; each batch is one workstream. Not in scope: any model, memory between messages, streaming — that is the item below, which should now be read as version 2 and reuse everything built here. **Progress: batch 1 shipped 2026-09-23** (hospital send + return, weight, and the read-only lookups — plus the slide-over, the audit writes, the manual topic and the demo-badge removal). `demo-parser.ts` is now one parser per intent under `src/lib/assistant/intents/`, tried in the order `parse.ts` lists, so the next batch is a new file there plus a card and an action. **Next: batch 2, death + rehome**; then batch 3, immunization.
- [ ] **Doctor name through the assistant's vet booking.** Raised 2026-09-24 when the doctor-name feature shipped: `/vet-visits/new` and Edit take an optional doctor (`vet_appointments.doctor_name`, 0074), but the assistant's vet intent (`src/lib/assistant/intents/vet.ts`) strips "dr", "dr." and "doctor" as `VET_STOPWORDS` when matching the vet, so "book a vet visit for Panda with Dr Somchai" books the visit and drops the name. Accept it, but don't ask for it: parse a trailing "with Dr X" / "หมอ X" into the draft, show it on the preview card as an editable, optional field, and write it the way `bookVetVisit` does (see `docs/decisions.md`, 2026-09-24). If a schema PR touches vet visits first, fold a `p_doctor_name` parameter into `schedule_bulk_appointments` then, so both callers write the doctor in the RPC's own transaction.
- [ ] **A volunteer turned away from an assistant write leaves no audit row.** Noticed building batch 1 (2026-09-23). Volunteers get the read-only lookups; if one types a write request the browser answers "that one is for staff and management" and nothing reaches the server, so `assistant_actions` (0070) never sees it. Those are worth having — they say what the people with the least access actually want the assistant to do, which is exactly what should steer whether volunteers get more of it. The status enum is created in `0070_assistant_actions.sql` as `('confirmed','cancelled','unmatched')` and none of them is true of a refusal, so a `refused` value has to be added before the client can record it. **Two constraints, both confirmed 2026-09-23 — read them before planning this:** (1) it is **two migration files, not one**, per `docs/decisions.md` (PR #50, "An enum value and the code that uses it need two migration files"): `alter type … add value` cannot share a transaction with anything that uses the new value, and `apply-migrations.mjs` wraps each file in its own `begin…commit` — so the `alter type` goes alone, then whatever references it, and `--dry-run` of the second file fails until the first is really committed (move the second aside, apply the first, restore it, dry-run, apply). (2) CLAUDE.md allows only **one in-flight branch carrying a migration**, and `claude/cashflow-schema` holds that slot with 0071 — so this either waits for that to merge or folds into the same schema PR. It cannot be a parallel schema stream. Small, and not urgent — but the longer it waits the bigger the hole in the corpus.
- [ ] **In-app chat assistant that does things, not just answers.** Raised 2026-09-22: the Director's biggest pain point is time, and the ask is an experience like working with Claude here — type "move Panda from A3 to B1" or "Bella died last night, lung problems" and the system makes the changes, popping a modal only when it needs something it can't infer. **Hosting (user's direction 2026-09-22):** the production assistant should run on an LLM the shelter hosts itself — on the Raspberry Pi 5 at home or a cloud box — not on a metered Claude subscription. Claude is for the prototype and for teaching: build against the Claude API first because it gets tool use right with the least effort, and while doing so record every real exchange (request, tool calls, corrections) into an eval set that the self-hosted model is then measured against and, if needed, fine-tuned on. To keep that swap possible the model layer must be **provider-neutral from the first commit**: one `src/lib/assistant/llm.ts` adapter that takes messages + tool definitions and returns text / tool calls, with a Claude implementation (`@anthropic-ai/sdk`, `claude-opus-5`, streaming, adaptive thinking; `ANTHROPIC_API_KEY` as a Cloudflare secret) and an OpenAI-compatible implementation for whatever the shelter hosts (Ollama / llama.cpp / vLLM all speak that shape), selected by env var. Nothing outside the adapter may import a vendor SDK. **Honest sizing note for the Pi:** a Pi 5 with 8–16 GB runs a 3–8B quantised model at a few tokens/second; that is workable for a short reply but tool-calling accuracy in mixed Thai/English at that size is marginal and each turn will take tens of seconds. Expect the self-hosted target to be a small cloud GPU (or a hosted open-weight endpoint) and treat the Pi as the stretch goal — measure both against the eval set before deciding. **Shape:** a chat panel (slide-over from a header button, also a full page `/assistant`; works on the phone) that sends the conversation to a route handler which calls the adapter with a **fixed toolset that wraps the app's existing server-side operations** — never raw SQL. First tools: find resident (by name/Thai name/code, returning candidates), find enclosure, move resident (`src/lib/placements/move.ts`), send to / return from hospital (`hospital.ts`), record death (`deceased.ts`, with cause), foster/adopt/return (`rehome.ts`), log weight, log vet visit, log immunization, add prescription, and read-only lookups (resident hub summary, who is in an enclosure, what's due). Each tool runs under the caller's own Supabase session, so RLS and the role checks apply exactly as in the UI and the assistant can never do more than the person could by clicking. **Confirmation:** every write is a two-step — the model calls the tool with `dryRun: true`, the UI shows a preview card ("Move Panda (R-0042) from A3 → B1 today, no notes") with Confirm / Change / Cancel, and only a Confirm re-invokes the tool for real; irreversible ones (death) use the existing `ConfirmDialog` wording. This matters doubly for a smaller self-hosted model: the confirm step is what makes a mis-parsed request harmless. **Missing information:** a `request_details` tool the model calls when a required field is unknown (which of two residents named Panda; date of death when "last night" is ambiguous across midnight; the enclosure when only a zone was named) that renders a small modal form built from the tool's field spec; the answers go back as the tool result. **Ambiguity:** resident lookup always returns candidates with code, enclosure and photo so the model can ask "the Panda in A3 or the one in Foster?" instead of guessing. **Audit:** log each assistant conversation and every executed tool call (who, when, input, result, which model served it) to a new `assistant_actions` table, and stamp the resulting rows' notes with "via assistant" so the hub history shows it — this table doubles as the eval/training corpus. **Scope of first cut:** the placement and medical tools above, English + Thai input, admin/management/staff only (volunteers get read-only tools), no memory across conversations, Claude behind the adapter. **Second cut:** stand up the self-hosted model, run the eval set, switch the env var when it clears the bar. **Later:** voice input on the phone, "what happened this week" summaries from the management dashboard queries, drafting the Facebook post for a project. **Decide with the user before building:** whether low-risk logs (weight, immunization) may skip the confirm to keep it fast; the accuracy bar the self-hosted model must reach on the eval set before it replaces Claude; whether conversations are retained beyond the audit rows.

## Auth

## Architecture

- [ ] **`prescriptions` and `resident_diets` have no `updated_at`, so a wrong `end_date` is permanently undetectable.** Raised 2026-09-23 by the `utc-date-audit` stream. The audit could check `date` columns against `created_at` — a timestamptz, and therefore correct whatever the session timezone was — and that is how it established what the UTC "today" bug actually touched. `end_date` cannot be audited the same way: it is written days or weeks after the row is created, so `created_at` says nothing about when it was set, and neither table records when a row was last changed. Any `end_date` corrupted by the seven-hour window (from "End today", or from the deceased cascade above) is therefore unrecoverable after the fact — there is no reference timestamp to compare against. Add `updated_at timestamptz not null default now()` to both tables with a touch trigger, so the same audit is possible going forward. Additive and harmless to existing code; goes in the same migration PR as the `current_date` item above if that one is done first, since both are small.

- [ ] **Promote the `test-plan` check from a red flag to a required check.** Requested 2026-09-23 by Lutan, alongside enlisting a test manager: every feature gets a test plan checklist, filled in during verification and signed off before the feature reaches production. The mechanism is built — `docs/test-plan-template.md`, copied per feature to `docs/test-plans/<feature>.md`, plus `scripts/check-test-plan.mjs` and a `test-plan` CI job that fails a PR which does not add or update a *completed* checklist (every line ticked or `n/a: <reason>`, no placeholders left, `Result:` one of three values, `fail` blocks). That is PR #55 — open, CI green, carrying its own completed checklist as the first one through the gate. **The part deliberately not enabled yet:** `test-plan` is not a required check in branch protection on `main`, so it reports red and the PR merges anyway. That is a GitHub settings change on the repo (Settings → Branches → `main` → require `CI` *and* `test-plan`). **No exemptions, including schema-only PRs** — ruled 2026-09-23: an exemption was granted and then reversed within minutes, on the grounds that a checklist which is mostly `n/a: no UI surface, no code reads these columns yet` costs about a minute and is exactly the record you want when that migration later turns out to matter. If it is ever revisited, derive the exemption from the diff (the PR touches nothing outside `supabase/migrations/`) rather than from a label: a label is a manual click that can land on a PR which is not schema-only, so it has to be verified against the file list anyway, at which point the file list is doing the work and the label only adds a way to get it wrong. Note also that the gate sits before merge, not before deploy — `main` moves several times a day (four merges on 2026-09-23 alone), so untested work accumulating on `main` would make every release all-or-nothing. Keep a thin release-time smoke list as well (sign in, residents list, one resident hub, `/adopt` signed out), because a per-feature gate cannot catch two features interacting badly. Everything merged up to `f32f2c1` (#51–#54) is the agreed baseline and is exempt; testing starts from the next feature. **Staged deliberately, 2026-09-23:** Lutan's call is to run `test-plan` as a red flag first and promote it to a required check once the process has visibly bedded in — "see how we go, and if it is working smoothly then we can change to a hard block". So red-without-blocking is a planned first stage, not an oversight, and this item is the scheduled revisit rather than a defect. The reasoning (a soft gate people follow beats a hard gate they resent, and nothing is live until the Pi lands) is recorded in `docs/decisions.md` on PR #55.

- [x] **`check-test-plan.mjs` can't tell a post-merge sign-off PR from a feature PR that forgot its release line.** Raised 2026-09-24 on PR #80. #77 (intake capacity warning) merged with its plan complete apart from a real-phone check; the phone pass was then recorded in a follow-up PR from the same branch that touched only `docs/test-plans/intake-capacity-warning.md`. The checker reads the plan's **Release notes.** line against *that PR's* diff, and a ticked line with no new `unreleased` entry fails ("gained no line in this PR"), even though #77 had added it. The workaround was to reword the line as `n/a: in this follow-up PR … went into unreleased with #77` — honest, but it rewrites the feature's permanent record to suit a later PR, and every sign-off recorded after merge will hit the same thing. This will be common: section 8's deployed smoke test can only happen after merge, so a manual check moved there (as the phone pass was) is always recorded post-merge. Fix options: (a) when a PR changes only files under `docs/test-plans/`, check the release-notes line against the diff that introduced the plan (`git log --diff-filter=A` on the file, or the first PR that touched it) rather than the current diff; (b) accept a ticked line when `unreleased` — or a cut release — already contains a line the plan quotes; (c) an explicit marker such as `release line: #77` that the checker verifies by looking at that PR's diff. (a) needs no new syntax and is probably the right default. Afterwards, restore #80's line to its ticked wording. Soft gate, so nothing is blocked meanwhile — but a false red on every sign-off PR is exactly what teaches people to ignore a red `test-plan`.

- [ ] **Top-to-bottom architectural review by Fable.** Requested 2026-09-22. Open a session on the Fable model (`claude-fable-5-1`) and ask for a read-only review of the whole system, not a feature: the Next.js app (`src/app` routes, `src/lib` services, `src/components`, `src/proxy.ts` auth/redirect handling), the Supabase schema, RLS policies and the migration history in `supabase/migrations/`, the Cloudflare Worker / OpenNext deployment and the environment split (`test.lannacare.org` vs `lannacare.org`, `scripts/deploy.mjs`), the operational scripts (`scripts/apply-migrations.mjs`, `scripts/backup.mjs`, `scripts/pi/`), the Google Drive integration, the public website vs the staff app, and the docs (`README.md`, `docs/decisions.md`, `docs/pi-hosting.md`, `docs/manual`). Ask for: (a) a plain description of the architecture as it actually is, to check against the README; (b) ranked findings — security and data-loss risks first, then coupling / duplication / anything that will hurt when the shelter grows or a second developer arrives, then nits — each with the file(s) and a concrete suggested change; (c) what is fine and should be left alone. Write the result to `docs/architecture-review-2026-09-22.md` (dated, so later reviews sit beside it) and turn each finding worth acting on into its own backlog item under the matching heading. No code changes in that session — the review is the deliverable.

- [ ] **Anon can read four internal views that bypass RLS. Revoke it.** Found 2026-09-24 while writing `0077_data_api_grants.sql` (see `docs/decisions.md`, 2026-09-24, "Data API grants"). With only the public anon key and no sign-in, `GET /rest/v1/current_placement`, `resident_current_state` and `immunization_compliance` returned rows on dev, and `immunization_duplicate_check` answers `200` too. They are owner-rights views (no `security_invoker`), so RLS on the base tables does not apply to them, and they kept the `anon` grants Supabase's old default privileges gave every new object. They expose resident names, status, deceased flag and date, placement notes and carer ids. Production is presumably the same, but it was not checked from a session. **Fix, as a schema PR:** `revoke all on current_placement, resident_current_state, immunization_compliance, immunization_duplicate_check from anon;`. Also consider `security_invoker = on` for any of them that signed-in roles should see only through RLS. While there, revoke the INSERT/UPDATE/DELETE/TRUNCATE that the same defaults left `anon` on every base table. RLS blocks those writes today, but TRUNCATE is not governed by RLS at all, so check whether `anon` holding it matters through PostgREST (it should not be reachable, but prove it). No app code reads these views as anon: the public site uses the `public_*` views and `site_content` / `site_content_photos` / `site_pages`. Then extend `scripts/check-public-views.mjs` to assert that anon is refused on every non-public view. Needs Lutan's go, then a production apply.

- [ ] **Full security review and cyber threat assessment of the platform.** Requested 2026-09-24 by Lutan. The deliverable is an **assessment report**: `docs/security-review-<date>.md`, dated so later reviews sit beside it. It maps every threat surface, says who could attack each one and how, rates each risk, and proposes mitigations. This is a separate item from the Fable architectural review above. That review ranks security first but covers the whole architecture. This one is only about security, and it asks "who would attack this, and what would they get?" not "is this well built?". Whichever runs second reads the other's report and does not repeat its findings. **Threat surfaces to cover, at minimum:** (a) **the public website and anonymous access**: `/adopt`, `/foster`, `/volunteer`, `/donate`, `/our-work`, `/r`, `/e`, `/privacy`; the anon key that ships in the client bundle by design; every table and view anon can reach through PostgREST, found by listing grants and `pg_policies`, not from `scripts/check-public-views.mjs`'s four-view allowlist (see the Salaries item: `site_content`'s unqualified `using (true)`); and spam and abuse on any public form; (b) **authentication and sessions**: Google OAuth (`src/app/auth/callback`), email/password with temporary passwords and `must_change_password` in `app_metadata`, the access-request queue (a stranger's Google sign-in leaves an auth row), archived users, the `src/proxy.ts` guards, `bootstrap-admin.mjs`, whether the pages under `/admin` and `/management` each check the role or rely on the nav hiding them, and MFA for admins; (c) **authorization and RLS**: role escalation (can a volunteer write `user_roles`?), `security definer` functions and triggers (0024, 0026 and later), write paths for each role, and service-role use in server actions; (d) **file handling**: the attachment and photo routes under `src/app/api/*` (upload type and size limits, who may read a `photos/[fileId]`, whether a Drive file ID can be guessed or enumerated) and the Google Drive service credentials, including what those credentials can reach beyond the shelter's folders; (e) **the assistant** (`/assistant`, `assistant_actions` 0070): prompt injection through resident notes, contact records or uploaded documents; what the assistant can read and write as the signed-in user; and whether its actions need confirmation; (f) **infrastructure and deployment**: the Cloudflare Worker (`worker/index.mjs`, including the Pi-origin fallback in `fetchFromOrigin`), `wrangler.jsonc`, the secrets in Workers and `.dev.vars`/`.env.local`, the dev/prod split (`test.lannacare.org` vs `lannacare.org`) and whether dev can ever reach prod data, `scripts/deploy.mjs`, the Pi host (`scripts/pi/`, `docs/pi-hosting.md`), security headers (CSP, HSTS, frame-ancestors) and Cloudflare WAF/rate-limit settings; (g) **the supply chain and repo**: npm dependencies (`npm audit`, lockfile integrity), GitHub Actions permissions in `ci.yml`, branch protection, the auto-push hooks, secrets in git history, and the Supabase Management API token that `apply-migrations.mjs` uses (who holds it, what it can do, and the 90-day token whose location is unknown); (h) **data protection and resilience**: what personal data is held (contacts, adopters, volunteers, and salaries if that item ships), backups (`scripts/backup.mjs`, `backup-schedule.ps1`: where they go, whether they are encrypted, whether a restore has been tested), ransomware or account-takeover recovery, and PDPA (Thailand) obligations; (i) **people and process**: phishing of staff and admin Google accounts, shared logins, offboarding (archive vs delete), the lannacare.org email routing catch-all as a spoofing and impersonation risk (SPF, DKIM and DMARC on the domain), and how much depends on one developer with many AI sessions (bus factor, and what an AI session is permitted to do). **For each finding:** the surface, a plausible attacker (opportunist or bot, disgruntled ex-volunteer, targeted attacker), likelihood × impact → Critical / High / Medium / Low, evidence (file and line, or the query that showed it), and a concrete mitigation with a rough effort estimate. Also list what is already done well, so nobody "fixes" it. Open with a one-page executive summary and a top-5 list that a non-developer (the Director) can read. **Rules of engagement:** read-only, plus hands-on testing **against dev / `test.lannacare.org` only**, never production (production reads are off-limits from sessions). Use the anon key and a test token for each role to confirm what RLS actually allows, not what the policies appear to say. Do not load-test or fuzz Cloudflare. Do not write out real secrets in the report: name where a secret lives, never its value. **Afterwards:** turn every Critical/High finding into its own backlog item (on the `backlog` branch), note Medium/Low in the report only, and add a `docs/decisions.md` entry on any risk that is deliberately accepted. The report goes out as its own PR with a test-plan checklist, which is mostly `n/a: documentation only`.

- [ ] **Enforce migration numbering with a check, not prose.** Raised 2026-09-22 after two in-flight branches both shipped `0069` (`0069_seed_standard_diet.sql` on the diet branch and `0069_assistant_actions.sql` on `claude/assistant-v1-schema`) and both were applied to dev before anyone noticed — the same clash as the two `0067` PRs earlier. CLAUDE.md already says the next number is one past the highest on `main` and that only one in-flight branch may carry a migration, but it is prose that nothing enforces, and `apply-migrations.mjs --status` reports `0 pending` while dev holds rows main has no file for, so the divergence is invisible. Add a check that fails at commit time (`.githooks/pre-commit`) and in CI: a new file under `supabase/migrations/` must be numbered exactly one past the highest on `origin/main`, and no two files may share a number. Worth pairing with a `--drift` mode on `apply-migrations.mjs` that lists rows in `schema_migrations` with no matching file — dev currently has **two**, not three: `--status` reported 72 applied against 70 files on `main` with `0 pending` (checked 2026-09-23), so exactly two recorded rows have no file. They are the two renumberings — `0067_public_resident_cards.sql` (now `0068`) and `0069_assistant_actions.sql` (now `0070`, commit `34bf49e`). `0069_seed_standard_diet.sql` kept its number and is still a file on `main`, so it is not an orphan. Both leftovers are harmless, but note that the count in this item was wrong until someone did the arithmetic by hand — which is the whole argument for the flag: an orphan count nobody can read off a command is an orphan count that drifts in the notes as well as the database.

- [ ] **Make production drift visible: nothing records what production has actually run.** Raised 2026-09-22, alongside the numbering item above and arguably the larger half of it. `schema_migrations` lives inside each database, so the repo has no idea what production has applied and `apply-migrations.mjs --status` only ever answers for the database it is pointed at. `0069_seed_standard_diet.sql` was applied to **production** while `main` had no file for it at all, and sat that way until PR #46 merged; dev meanwhile held three rows with no file on `main`, and `--status` cheerfully reported `0 pending` throughout. Drift is silent in both directions and nobody finds out until someone rebuilds a database or renumbers a file. Note the only thing that prevented a second production apply was accidental: `scripts/worktree.mjs new` copies `.env.local` but not `.env.deploy.production`, so a feature worktree physically cannot reach production — while the main checkout has the file and can. That is luck, not design. Worth: a `--drift` mode that diffs `schema_migrations` against the files on `origin/main` for a named environment and names both halves (applied-but-missing, present-but-unapplied); a `--status` that says which files on `origin/main` production has **not** run, so the pre-deploy gap is one command; and a loud refusal (or at least a prompt) when `--env production` is used from a branch whose migration is not yet on `origin/main`, since schema is supposed to reach `main` before any database. See `docs/decisions.md` 2026-09-22 for the 0069 clash this came out of.

- [ ] **`docs/decisions.md` merges by union locally but not on GitHub, so parallel PRs read CONFLICTING.** Raised 2026-09-22; cost a round trip on two PRs the same afternoon. `.gitattributes` marks the file `merge=union` precisely so parallel branches can each append without conflicting, and locally that works exactly as intended. GitHub's mergeability check does not apply `.gitattributes` merge drivers, so it sees both sides appending at the end of the file and reports CONFLICTING / DIRTY — on a branch whose local merge is clean and whose checks are green. PR #48 hit this, as did the reconciliation on #46. The workaround is to sync onto current `main` and push before opening the PR, so GitHub is handed a tree that already contains `main` and has nothing left to merge; that works, but it is a round trip per PR and it silently punishes whichever stream opens second. CLAUDE.md's "`docs/decisions.md` merges by union, so just append" is true of git and misleading about the PR page, so at minimum correct that sentence. Structural fix worth weighing: the file is 2085 lines and 78 entries, all append-only, so splitting it into `docs/decisions/<date>-<slug>.md` (one file per decision, an index generated or simply the directory listing) removes the collision entirely — two streams adding decisions then touch different files and neither git nor GitHub has anything to merge. Against that: every existing cross-reference says "see `docs/decisions.md`", the dated-prose format is genuinely pleasant to read end to end, and a one-file-per-decision tree loses that. A cheaper middle option is one file per month.

## Documentation

- [ ] **Review the manual against real use.** Have a staff member and a volunteer follow it on a phone for a day and note every step that didn't match, was missing (e.g. what to do when an upload fails, how to update a vet visit's status), or used a word the shelter doesn't. Fold the answers back into `en.ts` and re-run the screenshot script.
- [ ] **Thai edition of the manual.** A second content file (`src/lib/manual/th.ts`, same `Manual` shape) picked by locale in `src/app/manual/page.tsx`, with screenshots captured with the app switched to Thai (the script would need a locale flag and a `public/manual/th/` output folder). Wait until the English text has been through the review above so it's translated once.
- [ ] **Manual as a printable PDF.** Volunteers without a login (and the office wall) want a paper copy. `@react-pdf/renderer` is already a dependency for the resident summary, so a `/manual/pdf` route that renders the same `Manual` data is the natural route; the Thai fonts under `src/lib/archive/fonts/` cover the Thai edition.
- [ ] **Give the manual's table of contents its own scroll bar.** Customer request 2026-09-24: on `/manual` the Contents list and the text scroll together, so on a long page the contents are out of reach and anything below the fold of the list can only be reached by scrolling the whole page. The desktop `<aside>` in `src/app/manual/page.tsx` is already `lg:sticky lg:top-6` but has no height limit, so once the list is taller than the window it just scrolls away with the page. Cap it to the viewport (e.g. `lg:max-h-[calc(100dvh-…)]` allowing for the app header, plus `lg:overflow-y-auto` and `overscroll-contain`) so the contents stay put with their own scroll bar while the sections scroll beside them. Check sticky actually engages — if the app shell scrolls an inner container rather than the window, the offset and height must be measured against that — and consider scrolling the active entry into view in the list. The phone `<details>` version is unaffected. Update the manual's own description of the Contents panel in `src/lib/manual/en.ts` if it has one.
- [ ] **Deep links into the manual land short of their topic.** Found 2026-09-24 while verifying `claude/manual-toc-scroll`: a cold load of `/manual#getting-help` on dev (1400×900) ended with the section 1279px below the top of the window. The manual's screenshots are `loading="lazy"` `<img>`s with no `width`/`height` (`Screenshot` in `src/app/manual/page.tsx`), so they load after the browser's jump to the anchor and push the text down. Give each screenshot its intrinsic size (store `width`/`height` in `ManualScreenshot`, or read them in `scripts/manual-screenshots.mjs` when capturing) so the space is reserved, then recheck a deep link cold. Pre-existing; the Contents list itself highlights the right entry.
- [x] **Release notes as an accordion: version and title first, details on click.** Done 2026-09-24: each release is a `<details>`, newest open; `OpenReleaseFromHash` opens the release a `#v<version>` link targets; no Expand all yet (three releases; `docs/decisions.md`). Customer request 2026-09-24. `/releases` (`src/app/releases/page.tsx`) renders every release in full — version, title, date, Major and environment badges, then every note — so it grows by a screenful with each release (0.2.0 alone has seven long notes) and older releases are only reached by scrolling past everything newer. Make each release a collapsed row showing **version · title**, the date and the badges, which expands to its notes on click. Native `<details>`/`<summary>` is enough — keyboard and screen-reader accessible with no client JavaScript, so the page stays a server component; style the marker as a chevron that turns when open. Decide while building: (a) whether the **newest release starts expanded** (probably yes — it is the one people come to read) with the rest collapsed; (b) the existing `id="v<version>"` anchors must still land on an **open** release, since links such as `/releases#v0.2.0` would otherwise point at a closed `<details>` with its notes hidden — open the targeted one (a small client effect reading `location.hash`, including on `hashchange`); (c) the dev-only "Not released yet" block stays expanded as it is; (d) optionally an Expand all / Collapse all control once there are enough releases to want it. Check it on a phone, where the badges already wrap under the title. The manual's release-notes topic gets a line saying to click a release to see what changed, and the PR adds its own line to `unreleased`.

## Public website

- [ ] **Link to the LCA Facebook page from the public site.** Customer request 2026-09-22. The public footer (`src/app/adopt/PublicFooter.tsx`) already shows address, email, phone and a LINE link from `site_content`, but nothing points at the shelter's Facebook page, which is where most of their supporters actually are. Add `facebook_url` (and while there `instagram_url`, both nullable text) to `site_content` in a new migration, fields for them on the admin Website form (`/admin/website`, `StoryForm.tsx` / `actions.ts` — validate they are `https://` URLs on the right hosts), and in the footer render a small row of social icons (lucide `Facebook` / `Instagram`, `target="_blank" rel="noreferrer"`, `aria-label` from the dictionaries) after the LINE link, shown only for the URLs that are set. Put the same Facebook link in `PublicHeader.tsx` next to the language switcher on desktop only, and add `sameAs` with the URLs to the site's JSON-LD Organization block if one exists. The Facebook URL itself comes from the customer — leave the field empty until they supply it rather than guessing. Distinct from the low-priority "Publish to Facebook from the app" item below, which is about posting, not linking.
- [ ] **"Shelter Friends" — thank the businesses that help, on the public site.** Customer request 2026-09-24. Local companies help the shelter by donating goods (food, litter, medicine, building materials) or giving discounts (a vet clinic, a feed shop, a hardware store). The shelter wants to feature them publicly: who they are, what they do for the shelter, links to their website / Facebook page, and a map and contact details — both as a thank-you and so supporters can give them custom. **Build on contacts, don't duplicate them:** the business is already (or should be) a row in `contacts` — the Vendor it buys from, with phone, email, LINE, address and the resolved maps link from the contact-hub tidy-up — so a Friend is a contact with a public profile, not a second address book. Keep the curated public face in its own 1:1 table rather than more columns on `contacts`, so nothing private leaks by accident and the public view has an obvious boundary: **schema PR first** — `shelter_friends` (`contact_id` unique FK → `contacts`, `blurb` text through the translations table/queue like `site_pages`, `help_kind` (Donates goods / Discount / Services / Sponsorship — or free text, ask), `discount_note` e.g. "10% off for adopters, show your adoption card", `website_url`, `facebook_url`, `logo_drive_file_id` (Drive-hosted like the hero photo), `friend_since date`, `sort_order`, `published boolean default false`, and per-field opt-ins `show_phone` / `show_email` / `show_line` / `show_address` / `show_map`, all default false), admin/management RLS, and a `public_shelter_friends` view granted to `anon` that returns only published rows and only the opted-in contact fields — add it to `scripts/check-public-views.mjs`. A friend whose contact is archived (see "Archive contacts" under Management) drops out of the view. **Staff side:** a "Shelter Friend" card on `/contacts/[id]` (Make a Shelter Friend / edit profile / publish toggle / preview link), a Friend badge and filter chip on `/contacts` and `/management/contacts`, and a list on `/admin/website` (or Management → Shelter Friends) to reorder and unpublish. Validate URLs as `https://`, Facebook on facebook.com / fb.com — share the validator with the Facebook-link item above. **Contact type: stays `Vendor`** (decided 2026-09-24) — any business, including one that only donates, is entered as a Vendor; no enum change in this item. Keep the Friend profile keyed on `contact_id`, not on the type, so a type added later (e.g. `Business` / `Supporter`) can become a Shelter Friend with no further schema work — the Make a Shelter Friend action may be offered on Vendors only for now, but gate it in one place so widening it is a one-line change. **Public side:** `/friends` (title "Shelter Friends" / Thai to confirm with the customer) — a card per friend: logo, name, what they do for the shelter, discount note, website / Facebook icon links (`target="_blank" rel="noreferrer"`), phone / LINE tap-to-contact and the lazy-loaded map embed reused from the contact hub, only for what they opted into; linked from `PublicHeader` / `PublicFooter`, a small logo strip on `/` ("Thank you to our Shelter Friends"), and a mention on `/donate` ("give in kind — our friends' discounts go further"). `generateMetadata` with OG tags like the other public pages. **Consent:** the opt-in flags are the point — staff tick only what the business agreed to show, so the manual topic should say to ask first. **Link to Fundraising:** if donations are recorded later, an in-kind gift from a friend should be able to point at the same contact, so the two meet on `contact_id`. Manual topic, dictionary strings in both languages, a releases line and a `docs/decisions.md` entry (separate table vs. contact columns; why opt-in per field).
- [x] **Resident photo thumbnails: stop the ever-growing grid.** Done 2026-09-22: `src/components/ThumbnailStrip.tsx` (single-row scrolling strip) on the adoption and story galleries; the staff Photos tab shows 8 tiles with a "Show all (N)" toggle. Customer feedback 2026-09-22 (see Panda on lannacare.org): a resident with many photos shows every thumbnail at once and the block just keeps growing. Public page `src/app/adopt/[id]/PhotoGallery.tsx` already has a hero image but its thumbnail strip is `flex-wrap`, so it wraps into more and more rows; the staff Photos tab (`src/components/PhotoGallery.tsx`) is a plain grid with no cap. Replace both with a bounded layout: a single-row thumbnail strip that scrolls left/right (`overflow-x-auto`, snap, arrow buttons on desktop, swipe on phones, keep the selected thumbnail in view), or show the first N (say 8) with a "Show all (23)" toggle that expands in place — pick one and use it in both galleries so they feel the same. Prefer the scrolling strip on the public page (the hero stays put, the strip is secondary) and "show all" on the staff tab, where set-as-profile and delete need every tile reachable. Keep lazy loading so a long strip doesn't fetch every image through the proxy up front.
- [ ] **"Contact us about an animal" form (later).** Raised 2026-09-21 while reviewing the public site: a public form for reporting a stray, an injured animal or a welfare concern (what / where / photo / how to reach you) that lands somewhere staff see it — a `public_enquiries` table with an admin or management list, or simply an email to the shelter's contact address via the existing Gmail plumbing. Overlaps with the Lost & found item below; probably one form with a "what is this about" choice. Not started.
- [ ] **Lost & found report form (future).** RSPCA ACT has a public "report a lost pet" form. Out of scope until the core app is wired up, but a natural later public feature: a public form (species, description, photo, location, contact) writing to a `lost_pet_reports` table that staff review from an admin list, with a link to the intake flow if the resident arrives at the shelter.
- [ ] **Publish to Facebook from the app (low priority, discuss first).** Raised 2026-09-22: could a resident profile, a project story or a "pet of the week" be posted to the shelter's Facebook Page (feed post, photo, or a 24-hour Story) from inside the app? Technically yes — the Meta Graph API publishes to a Page the app has been granted (`pages_manage_posts` + a long-lived Page token held as a Worker secret; Page Stories have their own photo/video endpoint; Instagram Business publishing is a sibling if the Page has an IG account). Personal profiles are not publishable by API. The cost is not code but Meta's App Review (screencast, business verification of the Page; `/privacy` already exists) and a token that needs refreshing. Before building anything, decide between: (a) no API at all — a **Share on Facebook** button on `/adopt/[id]` and `/our-work/[id]` that opens the share dialog pre-filled with the public URL (the OG tags are already there, so the card renders well) and staff post from their own account; (b) Meta Business Suite scheduling straight from the Page, no app changes; (c) the full integration — an Admin → Website action that composes a post from a resident or project (photo, bilingual caption from `translations`, link back) and publishes to the Page, with a log of what was posted. Start with (a) if anything; only go to (c) if staff are posting several times a week and the copy-paste is the pain.


## Fundraising

- [ ] **Make `/donate` ask for money and record what arrives.** Customer request 2026-09-23. Today `/donate` is just one of the five `site_pages` slugs (0059) — a title and a body the admin types on `/admin/website`, rendered by `SitePageView` (`src/lib/site/pages.ts`) and translated through the manager's queue — so the shelter can *describe* how to give, but the site asks for nothing and the system records nothing. Note also that `contact_type` lost its `Donor` value in 0067 as unused, so a donor is currently not modelled at all. **Ask the customer before any schema:** which channels people actually use today — Thai bank transfer / PromptPay QR, cash at the shelter, a Facebook fundraiser, PayPal or card from overseas supporters — and who thanks them and keeps the list now. Then build to that answer, in this order. (a) **A give block on `/donate`** — a PromptPay QR image (Drive-hosted like the hero photo) plus bank account name and number and whatever overseas option they use, as nullable columns on `site_content` edited on `/admin/website` beside the contact block; no integration at all, covers most Thai giving, and worth shipping on its own. (b) **Record donations** — schema first, its own PR: a `donations` table (`amount numeric(12,2)`, `received_on date`, method, optional `contact_id` plus a free-text donor name for one-offs, optional designation to a resident or a project, note, `recorded_by`), an admin/management RLS pair, and a **Management → Donations** page to enter one, list a window and total it. That total is the income half the Cashflow forecast item above has no source for, so design the two to meet. (c) **Appeals with a target** — a campaign row (title, body, goal, deadline, cover photo) with a progress bar on `/our-work/[id]` and a "sponsor {name}" block on `/adopt/[id]`, summing the donations designated to it; the translation plumbing and the `site_pages` body renderer already cover the prose. (d) **Card / online payment**, only if the customer says they need it — a hosted checkout (Opn/Omise covers PromptPay and Thai cards, Stripe for overseas), keys as Worker secrets, a webhook route under `src/app/api` writing the donation row, and no card data ever touching the app or the Pi. Don't skip to (d): (a) and (b) are most of the value for none of the compliance. Manual topic, dictionary strings in both languages and a `docs/decisions.md` entry once the shape is settled.

- [ ] **Scope an online merchandise shop — the deliverable is a scope, not a build.** Customer request 2026-09-23. The ask is selling shelter merchandise online (t-shirts, calendars, tote bags and the like); this item ends with `docs/merch-shop.md` plus a `docs/decisions.md` entry and a recommendation the customer can say yes or no to — no schema and no routes until they have. **Questions to answer with them:** what is actually for sale and at what price; who packs and posts a parcel (shelter staff in Suphan Buri? Thailand Post / Kerry / J&T, who pays the postage, domestic only or overseas too?), or whether print-on-demand removes stock entirely; whether collection at the shelter is the common case; how many orders a month realistically — that number decides everything below; who answers "where is my order"; stock counts, sizes and variants, and what a sold-out item does; receipts, refunds, and whether the shelter's tax position cares that this is trading income rather than donations. **The real decision is build vs. link:** (a) point the public site at a hosted storefront — LINE MyShop or a Facebook/Instagram Shop, which is where their supporters already are, or Shopify — so the app adds a nav link and nothing else, and the shelter gets payments, stock and shipping labels for free; (b) build `/shop` into this app, reusing the Drive photo plumbing, the `site_pages` copy pattern, the translation queue and the Worker, with `products` / `orders` tables and a hosted checkout; (c) a middle path — a catalogue in the app that hands off to (a) for payment. Weigh it honestly: (b) is only worth the work if an order needs to tie back to something the system already knows (a resident, an appeal, a supporter record shared with donations), and it puts payments, stock and customer support on infrastructure that is about to move onto a Raspberry Pi in Suphan Buri (`docs/pi-hosting.md`), where an hour of downtime is a lost sale rather than a staff member waiting. Read it next to the Fundraising item above: if both happen, a buyer and a donor are usually the same person and should not end up as two unrelated tables.

- [ ] **Investigate options for a drop-shipment B2B offering on the system — the deliverable is an options paper, not a build.** Requested 2026-09-24 by Lutan. The idea: sell goods through the system without the shelter holding stock or posting parcels, a supplier shipping direct to the buyer, with businesses as the buyers or the partners. This item ends with `docs/drop-shipping-b2b.md` plus a `docs/decisions.md` entry and a recommendation Lutan and the customer can say yes or no to — no schema and no routes until they have. **First pin down what "B2B" means here**, because the options differ completely: (i) the shelter *sources* from a B2B drop-ship supplier (print-on-demand merch, or pet food and supplies from a Thai wholesaler) and sells on to supporters; (ii) the shelter *sells to businesses*: vets, pet shops, cafés and the companies on the "Shelter Friends" list buying branded or bulk goods, with the supplier shipping direct; or (iii) businesses sell *through* the shelter's site with a cut going to the shelter (an affiliate or marketplace model). **For each, answer:** which suppliers actually drop-ship inside Thailand (and overseas, if supporters abroad matter), their integration (API, CSV feed, email order, or a manual order form), margins and minimums; who owns payment, VAT invoices / tax receipts for business buyers, returns and "where is my order"; whether trading income changes the shelter's legal or tax position (the same question the merch item raises); and what the system would have to hold: products, business accounts, orders, supplier routing. **Weigh build vs. link honestly**, as the merch item does: a hosted storefront or the supplier's own white-label shop may give most of the value for none of the payments, support and Pi-uptime risk (`docs/pi-hosting.md`). Read it next to **Scope an online merchandise shop** and **Make `/donate` ask for money** above, and **"Shelter Friends"** on the public site: if more than one happens they should share one catalogue, one checkout and one record of who the buyer, donor or business is. Likely outcome is that this folds into the merch scope as one of its options, and that is a fine answer.

## Completed

Done items, kept under the same headings as the open work above so follow-ups can be traced back to what they build on.

### Process

- [x] **Run several workstreams at once.** Done 2026-09-22: one git worktree per feature (`scripts/worktree.mjs new|dev|sync|list|done`), the main checkout stays on `main`, `/plan-day` syncs `main` and proposes the day's two or three non-overlapping streams from this file, schema changes land first as their own PR, CI type-checks/lints/builds every PR, and `docs/decisions.md` merges by union. `CLAUDE.md` "Workstreams", README "Day-to-day workflow", `docs/decisions.md`.

### Deployment

- [x] **A third environment, `uat`, in the app and the deploy tooling.** Done 2026-09-23, plumbing only: `src/lib/app-env.ts` returns `dev | uat | production`, with `UAT_PROJECT_REF` left empty until the cutover sets it (it will be `dbkodyyxxhtygxcxmfcu`, production's today). UAT keeps production's colours; a **UAT** badge in `AppHeader` and a diagonal watermark on the archive PDF are the only differences (dev builds get a `DEV` watermark too, since they share UAT's Drive). `wrangler.jsonc` has a `uat` block (`lanna-animal-care-uat`, no routes yet), `--env uat` works in every `scripts/lib/env.mjs` script with `.env.deploy.uat`, `npm run deploy:uat` exists, and `deploy-pi.sh` knows its service and host. `deploy.mjs` now refuses any environment whose project app-env would badge as another, so `deploy:uat` refuses until the cutover, and UAT gets production's clean-main and written-release guards. No thin top strip: the badge was judged enough. README "Environments" lists what the cutover changes.
- [x] **Builds no longer type-check the dev server's `.next/dev/types`.** Done 2026-09-22: `tsconfig.build.json` (`tsconfig.json` minus `.next/dev`) is what `next build` type-checks with (`typescript.tsconfigPath` in `next.config.ts`, keyed on NODE_ENV so `next dev` and the editor keep `tsconfig.json`) and what `npm run typecheck` runs `tsc` against. Reproduced with truncated `routes.d.ts` / `validator.ts` under a running dev server: `next build` failed before, and `npm run opennext:build` (the deploy script's build step) completed after with the same files in place. Root cause is Next 16's `experimental.useTypeScriptCli` default, which hands the tsconfig straight to `tsc --project`; Next's own checker filters `.next/dev/types` out and the CLI path doesn't. Original brief: **A production build can fail type-checking on half-written `.next/dev/types/*.ts`.** Seen 2026-09-22: `tsconfig.json` includes `.next/dev/types/**/*.ts`, which a running `next dev` rewrites; when `next build` type-checks while that is mid-write (or after an interrupted build wiped `.next`), it fails with a wall of `TS1005 ';' expected` in `routes.d.ts` / `validator.ts` and the deploy stops. Workaround: delete `.next/dev/types/*` and rerun. Fix: have `scripts/deploy.mjs` build in a scratch copy of `.next` (or stop the dev server first / exclude `.next/dev/types` from the build-time `tsc`), so a deploy never depends on what the dev server is doing.
- [x] **Scheduled production backups.** Done 2026-09-21: `scripts/backup.mjs --env production` (`pg_dump` custom format of `public` + `auth` over the session pooler — the direct host is IPv6-only — into `Backups/` under the Drive root, newest twelve kept) and `scripts/backup-schedule.ps1` for the Sunday 03:00 Task Scheduler job; README "Backups". The restore rehearsal is its own open item above. Original brief: Free-tier Supabase has no backups. Write `scripts/backup.mjs --env production`: `pg_dump` (custom format) over the project's direct connection string, uploaded into a `Backups/` folder under the Drive root with the date in the name, keep the last N. Run it weekly from Task Scheduler on this machine (or a reminder) until something better exists. Test a restore into a scratch project once. Nothing destructive should happen on production until this runs.

### Quick wins

- [x] **"Today" is UTC everywhere, so it is yesterday in Thailand until 07:00.** Done 2026-09-24, in the three parts the item asked for. *The code*: `todayIso()` in `src/lib/format.ts` formats in the shelter's timezone (`SHELTER_TIME_ZONE`, `Asia/Bangkok`), and every one of the 27 call sites across 24 files now uses it. *The SQL*: Postgres had its own copy of the same bug — `current_date` in a UTC session — closed separately by `0073_shelter_today.sql` (`shelter_time_zone()` / `shelter_today()` / `shelter_date()`), which is its own completed item under Architecture. *The data*: `docs/utc-date-audit-2026-09-23.md` compared every `date` column against its `created_at` on both dev and production; the three real candidates turned out to be a single event, so nothing was bulk-corrected — the audit was brought to the user rather than acted on unilaterally, as the item asked. See `docs/decisions.md` (2026-09-23, shelter dates are Asia/Bangkok, not the viewer's clock and not UTC). **Ticked 2026-09-24 on the `backlog` branch:** the work merged that morning but no PR ticked the item, so it sat open for a day while three planning rounds flagged it as already finished.
- [x] **Fix mobile row click on residents list.** In `src/app/residents/ResidentsTable.tsx` the name link's `after:absolute after:inset-0` overlay relies on `relative` on the `<tr>`, which mobile WebKit ignores, so tapping anywhere opens the last row. Anchor it to the cell or make the row properly clickable.
- [x] **Add "Book vet visit" button to the resident hub.** Put it on the vet-appointments section (and stat card) linking to `/vet-visits/new?residentId=<id>`, mirroring how immunizations already do it.
- [x] **Show age on the public resident page.** Add `estimated_age_years` and `intake_date` to the `public_resident_profiles` view (new migration) and render an Age row on `/adopt/[id]` using the existing `formatAge()` helper.
- [x] **Strategy for free-text fields across languages.** Decided and built 2026-09-21 (see `docs/decisions.md`): public-facing prose (resident bio / temperament / past story, project story, photo captions) gets one row in a `translations` side table (`0056_translations.sql`) — the text in the other language, a status, and a snapshot of the source it was written against, so an edit to the original re-queues it as *Out of date* rather than leaving the Thai silently wrong. Source language is detected from the script of the text. Managers write and approve at `/management/translations` (stale first, editor open on every row) or in the same `TranslationPanel` under the field on the resident hub / project folder; approved text is what a visitor reading that language sees, else the original. `summary_th` / `caption_th` moved into the table and were dropped; `thai_name` / `name_th` stay as labels. Maintenance job title and description joined the same day (`0057_translations_maintenance.sql`: tiers renamed to `reviewed` / `machine`, board and job page show the reader's language) — the user's first question was whether tasks were covered. Internal notes are untouched until the machine phase (Management item above); zone and enclosure names are the Facility item below. Original brief: The UI is fully bilingual, but staff-typed text (weight and vet-visit notes, bio and behaviour notes, cause of death, prescription notes, intake notes) shows in whatever language it was typed — a Thai user sees English notes and vice versa (noticed 2026-09-20 on the Weight tab). Options to weigh: leave as-is with a house rule on which language notes are written in; paired `_th` columns where a field is public-facing (the bio already has precedent in `thai_name`); or on-demand machine translation shown as a hint under the original, never replacing it. Decide before the public site or foster portal shows free text to a different audience than the one that wrote it.
- [x] **Contact hub tidy-up (four customer requests, 2026-09-22).** Done 2026-09-22 — Donor/Other removed from the enum outright (migration 0067, see `docs/decisions.md`); the address line and a map preview replace the duplicated detail rows; shared `maps.app.goo.gl` links are resolved server-side. Original item: All in `src/app/contacts/[id]/ContactHub.tsx` unless noted; one branch, one PR. (a) **Drop the Donor and Other contact types.** `CONTACT_TYPES` in `src/lib/contacts/contacts.ts` and the `contact_type` enum (`0001`) both carry `Donor` and `Other`; remove them from the constant, the Management → Contacts form and the `t.contacts.types` labels (`en.ts` ~2550 and `th.ts`). Check the dev and production `contacts` tables for rows of those types first (`select type, count(*) from contacts group by 1`) and decide with the user what they become (Volunteer? Vendor?) before writing the migration — Postgres can't drop an enum value, so either leave the values in the enum and stop offering them (simplest) or recreate the type; say which in `docs/decisions.md`. `scripts/import-appsheet.mjs` maps AppSheet's Donor too. (b) **Phone and WhatsApp appear twice on the card**: `ContactActions size="lg"` already renders each channel as an orange tile with its value, and the `<dl>` beneath repeats the same values as text. Remove the duplicated text rows — keep the `<dl>` only for details that have no action tile (today that's none, since every detail has a matching action), or fold the value into the tile and drop the list; whichever, the address must stay readable in full (long Thai addresses are truncated in a tile). (c) **Map preview for the address.** `mapHref()` treats an `http(s)` address as a pasted maps link and everything else as a search query. When the address is a link (or, cheaper, always), show a small embedded map under the address: a keyless `<iframe src="https://www.google.com/maps?q=<query>&output=embed">` works without an API key for a search query; a shared `maps.app.goo.gl` short link can't be embedded directly (it redirects, and iframes don't follow it into a usable embed) — either resolve it server-side once and store the coordinates, or fall back to the search embed on the text after the link. Lazy-load the iframe, keep it ~200px tall, and don't render it in the `/contacts` list. (d) **Alignment between Residents in care and Past placements.** The two sections sit in a `lg:grid-cols-2`; the left one has the `residentsInCareDetail` helper paragraph under its heading and the right one doesn't, so the two lists start at different heights. Either give both sections a helper line (e.g. "Residents who used to live with this carer.") or move the helper below the list next to `assignFromResident`, so the headings and the first card line up.
- [x] **Pin User manual, Change password and Security to the bottom of the left nav.** Done 2026-09-22: `NavLinks` splits its list into `mainItems` and a `footerItems` group (User manual, Change password, and Security for admins) rendered after `mt-auto` and made `sticky bottom-4` — the sidebar is stretched to the height of the page, not the window, so `mt-auto` alone parked the group thousands of pixels below the fold on a long list. It now sits on the bottom of the window above a divider, and comes last in the phone drawer. Security left the Admin accordion, which made two knock-ons: `/admin` redirects to `/admin/website` (the first page the group still lists) instead of `/admin/security`, and a group counts as “inside” for auto-expand when the route matches one of its own children rather than the group href, so opening Security no longer springs Admin open. Manual’s navigation topic and the Security topic’s path updated. Original brief: Customer request 2026-09-22. In `src/app/NavLinks.tsx` the `/manual` and `/account/password` entries sit in the middle of `items`, between the Assistant demo and the Management group; Security is a child of the Admin accordion. Split the list into a main group and a footer group rendered after a `mt-auto` spacer so the footer sits at the bottom of the sidebar (the drawer on mobile can just list them last with a divider). Footer contents: User manual, Change password and — for admins — Security, which then leaves the Admin accordion (keep `/admin` itself listing it on its landing page). Update the manual's navigation topic (`src/lib/manual/en.ts`, `th.ts`).
- [x] **The pinned nav footer group hides the last Admin links when both accordions are open.** Closed 2026-09-23 by the nav rework rather than fixed separately: the Management and Settings children are gone, and the pinned group is now pinned only on windows at least 44rem tall (`[@media(min-height:44rem)]:sticky`). Even the flat list plus the group needs ~690px for an admin, so it still covered Settings on a 640px window when first built; below that height the group now follows the list behind its divider, as in the phone drawer. Bounding the sidebar with its own scrollbar (below) was not needed. Original brief: Noticed 2026-09-22 while building the footer group. `src/app/NavLinks.tsx` renders it `mt-auto sticky bottom-4 bg-surface`, which holds it against the bottom of the window — but the sidebar is a flex item stretched to the height of the *document*, so the links stay in the flow at the top while the group floats over them. An admin with Management *and* Admin expanded on a window under ~950px has 21 links plus the group: at the top of the page `/admin/zones`, `/admin/immunization-types` and `/admin/procedure-types` sit behind it, and since the divider looks like the end of the menu they read as missing rather than hidden. Scrolling ~200px clears them; one group expanded never overlaps. The proper fix is to bound the sidebar to the window and give it its own scrollbar (`md:sticky md:top-14 md:h-[calc(100dvh-3.5rem)] md:overflow-y-auto`), which needs `AppHeader` pinned at a fixed height and made sticky — a visible change on every page, so decide with the user first. Making the two accordions mutually exclusive would also do it without any layout change.
- [x] **Nav rework: rename Admin to Settings, drop the chevrons, reorder, add icons.** Done 2026-09-23: Management and Settings are single links to their tile-grid landing pages, so the ~13 child links, both chevrons, the `nav-expanded` sessionStorage and the auto-expand rules are gone from `src/app/NavLinks.tsx`. Order agreed with Lutan: Residents, Enclosures, Maintenance | Vets, Contacts, Projects | Assistant | Management, Settings, with a thin rule between groups, then the pinned footer. Every link has the icon its page already uses (`NAV_ICONS` in `src/components/hub-icons.ts`, which the Settings Security tile now shares). The active link is the longest matching href, so `/admin/security` lights Security and not Settings too. The rename is label-only — `/admin/*` URLs and the `admin` role keep their names (`docs/decisions.md`); nav, Settings landing heading, menu paths in `en.ts` / `th.ts` and the manual changed. Original brief:
  Requested 2026-09-23 by Lutan; this absorbs the earlier "collapse the accordions to plain links" follow-up, which was waiting on exactly this decision. Four parts, one stream:
  1. **Drop the chevrons.** `/management` and `/admin` are tile-grid landing pages now (built 2026-09-23), so the sidebar lists Management and Settings as ordinary links and the landing page is the menu. That removes ~13 child links, the two chevron buttons, the `sessionStorage` (`nav-expanded`) choices and the auto-expand rules in `src/app/NavLinks.tsx` — and closes the pinned-footer overlap item above as a side effect, so tick that one too.
  2. **Rename "Admin" to "Settings"** in the nav label, the landing page heading, the i18n dictionaries (`en.ts` / `th.ts`) and the manual. Decide whether the URL moves too: `/settings` with `/admin/*` redirects (as `/admin/vets` → `/management/vets` was done) is cleaner, but a label-only rename is much smaller — ask. The `admin` *role* keeps its name; this is the menu, not the permission.
  3. **Reorder the nav.** Propose an order grouped by how often each link is used (daily field work first — Residents, medical, Facility — then Management, Settings, and the pinned footer) and **agree it with the user before building**; a quick sketch of two orders is enough.
  4. **Icons beside every nav item.** The landing-page tiles already carry an icon each — reuse the same icon set (and the same icon per page) so the nav and the tiles match. Icons should also make a collapsed/narrow sidebar feasible later, but don't build that here.

  `NavLinks.tsx` is a conflict magnet, so give it a stream of its own with nothing else in it, and update the manual's navigation topic (`src/lib/manual/en.ts`, `th.ts`).
- [x] **The assistant still carries its own copy of the "not deceased" filter.** Done 2026-09-24: `src/lib/assistant/data.ts` (where the assistant query had moved) now imports `NOT_DECEASED`; it was the only literal copy under `src/lib/assistant/` and `src/app/assistant/`. Original item: Noticed 2026-09-23 while doing the hide-deceased list item, which moved that PostgREST expression into `src/lib/residents/status.ts` as `NOT_DECEASED` and pointed the residents list and the vet-visit and immunization pickers at it. `src/app/assistant/page.tsx` was being rewritten on another branch at the time, so its literal `.or("current_status.neq.Deceased,current_status.is.null")` was left alone. Swap it for the import once the assistant work has landed, so there is one definition of which residents are still with us.

### Mobile

- [x] **Collapsible mobile navigation.** Replace the fixed `w-48` sidebar in `src/app/NavLinks.tsx` / `layout.tsx` with a hamburger-toggled drawer below the `md` breakpoint so phones don't show only the nav bar.
- [x] **Icon-based mobile hub and residents list.** Hub stat cards, tabs and action buttons use lucide icons (`src/components/hub-icons.ts`, `ActionLink.tsx`); the phone residents list shows only ID + name with no checkboxes or zone/enclosure filters.
- [x] **Browse residents by enclosure.** New mobile-friendly views to navigate zone → enclosure → residents (replacing the desktop-only zone/enclosure filters and extra columns that are hidden on phones in `src/app/residents/`). Lives at `/enclosures` (zone chips, name search, sort by zone/name/fullest; capacity indicator per card) and `/enclosures/[id]` (occupancy bar, notes, resident thumbnails, disabled Maintenance placeholder).

### Resident operations

- [x] **Summary PDF: a stray hyphen where a long name wraps before a Thai word.** Done 2026-09-24: textkit hands the hyphenation callback one *run* at a time and starts a new run where the script changes, so `Valley (เจ้าหญิง…)` arrived as `(` and `เจ้าหญิง…)`; with no space between them the line breaker counted the join as a hyphenation point and printed `(-` when it broke there. Every `Text` in `src/lib/archive/resident-summary-pdf.tsx` now carries `hyphenationPenalty` of textkit's infinity (10000), which makes such points unbreakable, so lines break only at spaces. Verified by rendering long Thai and long Latin names before and after (`docs/test-plans/summary-pdf-hyphen.md`).
- [x] **Intake warns when the chosen enclosure is nearly full or full.** Done 2026-09-24: the wizard's Arrival step uses `EnclosurePicker` (idPrefix `intake`, with placeholders that keep "blank means Unassigned"), fed by `loadEnclosureOptions()` — the same loader as Move — so the headcount and thresholds cannot disagree between screens. Register opens `CapacityWarningDialog` (intake wording via a new `copy` prop) and, as everywhere else, only warns: Register anyway still registers. The warning is on Register rather than on Arrival's Next (see `docs/decisions.md`). Correction to the brief: `record_intake` never rejected a full enclosure, so before this a volunteer got no warning at all rather than a late error. Original brief: Noticed 2026-09-22 while turning `/residents/new` into a wizard. Every other placement screen — move, return from hospital, return to shelter, the edit form's housing section, maintenance — uses `EnclosurePicker` (`src/components/EnclosurePicker.tsx`), which shows the chosen enclosure's occupancy underneath and, with `CapacityWarningDialog`, asks before putting one more resident into a full or nearly-full one. Intake is the exception: it has its own plain Zone and Enclosure selects, so a volunteer registering an arrival at the gate — the person least likely to know which kennels are full — gets no warning, and the first they hear of it is an error from `record_intake`. Swap the two selects on the wizard's Arrival step for `EnclosurePicker`. It needs `residentCount` and `capacity` per enclosure (`src/lib/enclosures/options.ts`), which `/residents/new`'s page query doesn't select yet — copy the query the move page uses. Note the picker names its own controls (`idPrefix`), so the wizard's Review step, which reads answers back out of the form by control id, needs its zone/enclosure lookups updated to match.
- [x] **Deceased Resident Summary PDF: the name overlaps the subtitle, and the footer never renders.** Done 2026-09-24: one cause for both — the page style's unitless `lineHeight: 1.4`, which @react-pdf resolves to an absolute 12.6pt and hands down to every child (see `docs/decisions.md`). The 20pt name got a 12.6pt line box; the footer's page-number text, re-laid-out after its render prop runs, read the inherited 12.6 as a multiplier on each pass and came out ~5,300pt tall, so the bottom-anchored footer sat thousands of points above the page. Line height is now set per text style in `src/lib/archive/resident-summary-pdf.tsx`. Checked by rendering the real component against stub records (Latin, Thai, Thai-only, a name that wraps, a four-page DEV render) and reading every page. The six archives already in production keep the broken layout until each is regenerated — a separate backlog item. Original brief: Noticed 2026-09-23 while rendering the PDF for the UAT watermark. The 20pt name and the `R-0042 · Dog · …` subtitle print on top of each other, and the fixed footer (name, code, generated date, page N / M) is missing from every page. Production output shows both, so they predate the watermark. A render of the component against a stub record is enough to check a fix; no Drive or database needed.
- [x] **Hide deceased residents from the residents list by default, with a "Show all" toggle.** Done 2026-09-23: `/residents` filters `resident_list_view` with the shared `NOT_DECEASED` expression (`src/lib/residents/status.ts`, now also used by the vet-visit and immunization pickers), and `?all=1` — the Show all / Hide deceased chip beside the filters, on phones too — brings them back, dimmed, with Deceased in the Status column. The line under the heading counts the mode: "212 residents · 38 deceased hidden", or "including 38 deceased" with the toggle on. A name search that matches a deceased animal says so rather than switching the list out from under you — "1 deceased resident matches — show" (see `docs/decisions.md`). The deceased count respects the name / zone / enclosure filters, so it always counts the animals that list would have shown. `/enclosures/[id]` is unchanged by design.
- [x] **Step-by-step intake instead of one long form.** Done 2026-09-22: `/residents/new` now asks the same questions over five steps (Who, Arrival, Health, For adopters, Story) and a Review that lists every answer with an Edit link back to its step. The write is untouched — one `recordIntake` call to the `record_intake` RPC on the single Register press (see `docs/decisions.md`). Steps are hidden rather than unmounted so values and `required` survive; every forward move runs `checkValidity()` over the steps it passes so the browser's own message lands on the field at fault, and Register validates before `requestSubmit()`. `?step=` keeps the place across a refresh, never the answers. The adoption recommendation fields got a step of their own (the brief named four field groups but five steps); `EditResidentForm` stays flat. Manual's intake topic rewritten. *Still to do:* re-capture `public/manual/resident-intake.png` — it needs a signed-in browser against a dev database, so it is a run of `node scripts/manual-screenshots.mjs` rather than a code change. Original brief: Customer request 2026-09-22: `/residents/new` is intimidating — `src/app/residents/new/IntakeForm.tsx` shows all four fieldsets (identity with name / species / sex / size / age / weight / blood-test interval / diet; adoption profile; arrival with date / origin / zone + enclosure / notes / ready-for-adoption; bio with temperament / past story / behaviour) on one page, ~25 fields before the Register button. Turn it into a wizard: one section per step, a progress header ("Step 2 of 4 · Arrival", steps clickable once visited), Back / Next, and a final **Review** step that lists every answer with an Edit link back to its step before the single Register press. **Keep the write exactly as it is:** one `recordIntake` call to the `record_intake` RPC at the end (`actions.ts`), so nothing half-registered is ever created — the steps are a client-side view over one `<form>`, not separate submissions. Implementation shape: keep everything in the one `<form action={formAction}>` and hide the inactive steps with `hidden` (not unmounted, so their values and the `required` attributes survive), and run `checkValidity()` on the visible step's fields on Next so a step can't be left with its required fields empty — the browser's own validation messages then point at the right control. Order the steps so the required things come first and the optional prose last: 1 *Who* (name, Thai name, other names, species, breed, sex, size), 2 *Arrival* (date, origin incl. the add-origin toggle, zone → enclosure with the existing capacity note, ready for adoption), 3 *Health* (age, weight, blood-test interval, diet), 4 *Story* (bio, temperament, past story, behaviour — all optional, say so, and note they can be filled later from Edit resident), 5 *Review*. Keep the URL at `/residents/new` (a `?step=` param is fine so a refresh doesn't lose the place; the values themselves live in the form, not the URL). Phone first — this is the form a volunteer fills at the gate — so one column, big Next button pinned at the bottom, and check it in the phone drawer's width. The `IntakeState` error from the server (name taken, enclosure full, RPC failure) should land the user on the Review step with the message, not on step 1. Reuse `AdoptionProfileFields` unchanged. Leave `EditResidentForm` as the flat form it is — editing one field on a wizard is worse, and the two forms already share the field components. Update the manual's intake topic (`src/lib/manual/en.ts` `id: "intake"`, and `th.ts`) and re-capture `resident-intake.png` (or whatever the screenshot script names it) since the page looks different. Record in `docs/decisions.md` that the wizard is presentation only and the RPC is unchanged.
- [x] **Decide which fields stay editable after death.** Decided 2026-09-21: the bio group (bio, temperament, past story, behaviour notes) and photos (add, remove, choose the profile photo) — nothing else. `0052_deceased_editable_fields.sql` relaxes `enforce_deceased_lock()` for a `residents` UPDATE that changes only those columns and `enforce_deceased_lock_attachment()` for `'resident'` attachments; blood test and procedure files stay locked. The edit page shows only the photo and bio sections for a deceased resident, the Photos tab keeps its uploader, and `refreshDeceasedArchiveIfNeeded()` regenerates the summary PDF and offline index after each such change (best effort — the hub's Retry still covers a Drive failure). Original brief: The deceased lock in `0026_deceased_workflow.sql` currently allows nothing, pending that decision. Once the list exists, relax `enforce_deceased_lock()` for those columns (and re-generate the archive when one of them changes).
- [x] **Edit resident details.** Add `/residents/[id]/edit` with a form + server action to update identity/bio fields (name, Thai name, other names, animal code, species, breed, sex, age, notes, adoption/public flags). Housing is not edited here.
- [x] **Move resident between enclosures.** `/residents/[id]/move` (linked from the hub's Housing card and the housing section) and a Housing section on the edit form both insert a `ChangeEnclosure` row into `placement_history` via `src/lib/placements/move.ts` (zone → enclosure picker, date, notes; Lifecycle pseudo-enclosures excluded; capacity warning before moving into a nearly-full/full/over-capacity enclosure). Existing trigger closes the prior placement (0024 makes it `BEFORE INSERT` — it never could have fired past the one-active-placement index as `AFTER` — and `security definer` so volunteers' moves work).
- [x] **Send to hospital.** `/residents/[id]/hospital` (from the hub's Housing card, the housing section, and each vet visit record via `?vetAppointmentId=` which pre-fills date and notes) inserts a `SendToHospital` placement into the Lifecycle/Hospital enclosure via `src/lib/placements/hospital.ts`, storing `previous_enclosure_id` so the resident can return later. Admin/staff only. The Housing card reads "In hospital · Off-site in medical care · Returns to X" while they're there.
- [x] **Return from hospital.** `/residents/[id]/hospital/return` (from the hub's Housing card and the housing section while hospitalised — it and "Send to hospital" are opposites, only one is ever shown) inserts a `ReturnFromHospital` placement via `returnResidentFromHospital()` in `src/lib/placements/hospital.ts`. The zone → enclosure picker defaults to the stored `previous_enclosure_id` but any physical enclosure can be chosen (e.g. isolation first), with a notice when the previous one is gone or wasn't physical. Admin/staff only. "Move enclosure" is hidden (and rejected server-side) while in hospital.
- [x] **Foster and adopt.** `/residents/[id]/rehome` (from the hub's Housing card and the housing section; `?type=foster|adopt` picks the tab) inserts a `Foster` or `Adopt` placement into the Lifecycle/Fostered or /Adopted enclosure with `carer_id` via `rehomeResident()` in `src/lib/placements/rehome.ts`. The carer is picked from contacts of type Carer, or added inline (name, phone, email, LINE ID — saved as a Carer contact) until Contacts management exists. A fostered resident can be adopted, moved to another carer, or sent to hospital; an adopted one can only be returned. `/residents/[id]/rehome/return` inserts a `ReturnToShelter` placement into a physical enclosure (`returnResidentToShelter()`), defaulting to the enclosure they left from. Admin/staff only. Which actions show for each status lives in `src/lib/placements/available.ts`.
- [x] **Hide adopted residents from the public adoption pages.** `0025_public_views_exclude_adopted.sql` filters `public_resident_profiles` and `public_resident_photos` on `current_status not in ('Deceased', 'Adopted')`; fostered residents stay listed (foster-to-adopt is common). Same migration revokes everything but SELECT on both views from `anon` / `authenticated` — Supabase's default privileges had granted INSERT/UPDATE/DELETE, and `public_resident_profiles` is auto-updatable, so anonymous writes through it were being accepted.
- [x] **Undo a death recorded by mistake.** Done 2026-09-21 (see `docs/decisions.md`): the deceased banner offers an admin **Withdraw this death** → `/residents/[id]/deceased/undo` (reason required, confirmation dialog). `undo_deceased_placement()` (0049) appends a `DeceasedInError` placement (0048) back into whatever the death closed — the resident's history keeps both the death and the reversal, dated when it was made — and restores exactly what the cascade did from the snapshot the Deceased row now records in `placement_history.deceased_cascade`: the appointments it cancelled, the prescriptions it ended (with the end dates they had) and `ready_for_adoption`. The lock lifts on its own. The Drive folder moves back under `Residents/` and the generated summary PDF and index page are deleted (`src/lib/archive/restore-deceased-resident.ts`), with a retry on the hub if Drive failed. The dashboard no longer counts a withdrawn death. Original brief: Today a `Deceased` placement can only be reversed in SQL: the record is locked database-side and the cascade (cancelled appointments, ended prescriptions) isn't reversible row by row. Decide whether admins get a proper "recorded in error" path, and what it should restore.
- [x] **Record deceased.** `/residents/[id]/deceased` (from the icon beside the edit pencil on the hub's details card, admin/staff only) records the death (date, cause, notes) behind a confirmation dialog via `src/lib/placements/deceased.ts`. The resident becomes Deceased, drops off the public pages, and their whole record turns read-only — enforced by triggers in `0026_deceased_workflow.sql`, not just hidden buttons. The same migration fixes `current_status` never having returned `'Deceased'` (it tested the zone name, so `0025`'s public-view filter wasn't actually hiding deceased residents) and makes the 7.2 cascade `security definer` so staff's appointment/prescription cleanup isn't silently filtered out by RLS. Afterwards the Drive folder moves to `Residents/Deceased/`, and a summary PDF plus an offline `index.html` index are written into it (`src/lib/archive/`); the hub shows the archive state with a retry when Drive was down.

### Medical records

- [x] **Optional doctor name on a vet appointment.** Done 2026-09-24 in two PRs. #76 added `vet_appointments.doctor_name` (0074: free text; a trigger trims it and stores blank as NULL, and a check constraint states the same). The feature PR adds an optional **Doctor** field to `/vet-visits/new` and `/vet-visits/[id]/edit`. As decided 2026-09-23 it is free text with a `<datalist>` of the names already recorded against the chosen vet, loaded once with the page and following the vet select. The name shows after the vet on the resident's Vet Appointments tab and in the deceased archive (summary PDF and offline index), and after the reason on the vet hub's visit list. A visit with no doctor renders exactly as before. `schedule_bulk_appointments` did not gain the `p_doctor_name` parameter this item planned; the booking action sets the name on the rows the RPC returns instead (see `docs/decisions.md`). The assistant's vet booking neither asks for nor accepts a doctor yet. Original brief: Customer request 2026-09-23: some clinics put a particular doctor on a particular animal, and staff want the record to say who actually saw the resident. `vet_appointments` today records the clinic (`vet_id`), date, status, reason, notes and cost (0053) — there is nowhere to put a person.
- [x] **Medication requirement page.** Closed 2026-09-21 with the window picker: `/management/medications` (and `/management/diets`, same shape) shows each row's forecast for the next 7 and 30 days and, when From/To dates are entered above the table, a third column for that period (`ForecastWindowPicker`, a GET form; `src/lib/management/forecast-window.ts` parses and bounds it to a year). The days-of-stock projection is its own open item now, since nothing records stock on hand. Original brief: `medication_daily_requirement` (0027) already totals each medication's daily consumption across current prescriptions for living, non-adopted residents. Surface it — probably beside the immunization forecast once that exists — with a days-of-stock projection if a stock-on-hand figure is ever recorded.
- [x] **Dietary requirements.** Done 2026-09-21 as dated records, not a field (see `docs/decisions.md`): `0051_diets.sql` adds `residents.size` (Small / Medium / Large — required on the intake and edit forms, nullable in the database for residents intaken before it, shown on the hub and the public adoption pages), `diet_types` (Management → Diets: unit, cost per unit in baht, daily quantity per size) and `resident_diets` (diet, start / end date, meals a day, optional per-resident quantity override, notes; several may run at once; locked with the record on death). The hub gets a Diet card and a Diet tab (Current / Past, Add / Edit / End today like prescriptions), `/diets/new` and `/diets/[id]/edit`, intake takes the size and an optional starting diet, and `diet_forecast(from, to)` feeds a 7 / 30-day quantity and cost forecast with a total on the Management → Diets page, excluding fostered, adopted and deceased residents. Still open: a feeding sheet / enclosure view for volunteers on rounds, and Thai for free-text notes per the Quick Wins item. Original brief: Add a dietary requirements field/section to residents (free text or structured), editable from the hub and visible on the resident info tab.
- [x] **Blood test types.** Done 2026-09-21: `0050_blood_test_types.sql` adds the table (read for every role, write for admin), seeds the seven panels, adds `blood_tests.blood_test_type_id` NOT NULL with every existing row backfilled to CBC (see `docs/decisions.md`), and a `merge_blood_test_type` helper. `/admin/blood-test-types` (Admin nav, manual entry) is the procedure-types page with the names changed — add / rename / merge, delete blocked while any blood test references the row. The blood test form has a required Test type dropdown defaulting to CBC (no inline add — the list is short and the admin page exists from day one); the Blood Tests tab, the archive PDF and offline index show the type. Original brief: New `blood_test_types` table + admin CRUD page, `blood_test_type_id` on `blood_tests`, and a type dropdown on the blood test form defaulting to CBC. Seed: CBC (Complete Blood Count), Blood Chemistry Panel, Thyroid Panel, Heartworm Test, Tick Borne Disease Panel, Cortisol Test, Urinary Analysis.
- [x] **End or edit a prescription.** Done 2026-09-21: every row on the Prescriptions tab has an **Edit** link to `/prescriptions/[id]/edit` (the same `PrescriptionForm`, now in `src/app/prescriptions/`, in edit mode with a Cancel link), and current rows whose course has started have an **End today** button that sets `end_date` to today in place. No migration — 0027 already gave staff, vets and admins update on `prescriptions`, and its end-after-start check is why End today is withheld from a row dated to start later (edit it instead). Original brief: A vet stopping a course early, or a typo in the dose, currently means a new row: there's no edit page and no "end today" action on the Prescriptions tab. Add both (an `end_date = today` shortcut on current rows, and an edit form reusing `PrescriptionForm`), keeping the end-after-start check in mind.
- [x] **Prescriptions.** `/prescriptions/new?residentId=…[&vetAppointmentId=…]` (from the hub's prescriptions card, the Prescriptions tab, or an "Add prescription" link on each row of the Vet Appointments tab) records medication, dose, frequency, start/end date, linked visit and notes; the tab lists them as Current vs Expired. `0027_prescriptions.sql` adds what the AppSheet shape was missing — a `dose_unit` per medication (tablet, ml, mg, drop, …, so IV fluids and suspensions fit), `dose_quantity` and `vet_appointment_id` on the prescription, `doses_per_day` on frequency, an end-after-start check, staff write access, and a `medication_daily_requirement` view that excludes deceased/adopted residents. The deceased cascade already ended open prescriptions on the date of death; it now ends a future-dated one on its own start date instead so the new check can't roll a death back.
- [x] **Medication management.** Done 2026-09-21 under Management, not Admin (see `docs/decisions.md`): `/management/medications` lists medications (name, unit, doses and quantity due in the next 7 / 30 days from `medication_forecast`, prescription count) and the frequency options (label, schedule, prescription count) with add / rename / fix-unit / fix-doses-per-day inline, delete blocked while any prescription references the row, and **Merge…** to fold a duplicate into the one to keep (`merge_medication` / `merge_frequency`, 0043 — one transaction; medications only merge within the same unit). 0043 gives `management` read/write on both tables; 0044 turns frequencies into whole-number schedules (times a day, or every N days/weeks/months from the start date) so weekly and monthly tablets forecast as whole doses on the days they fall. Original brief: Admin page for medications and frequencies. Both can be added inline from the prescription form (vet/staff), but only an admin can rename one, fix a unit or `doses_per_day`, or merge a duplicate, and today that's SQL. Same shape as `/admin/immunization-types`.
- [x] **Weight.** Form + hub section to log and list weight entries (date, kg) against the existing `weight` table, newest first. Done with a trend chart, entry from the hub, vet visits and intake, and `vet_appointment_id` on `weight` (0028/0029).
- [x] **Procedures.** `/procedures/new?residentId=…[&vetAppointmentId=…]` (from the hub's Procedures card, the Procedures tab, or a "Log procedure" link on each vet appointment row) records type, date, linked visit and notes; the tab lists them with their files. `0031_procedure_types.sql` replaces the free-text type with a seeded `procedure_types` lookup (extendable inline), lets staff log procedures (on-site jobs like nail clipping), and adds `'procedure'` to `attachment_owner_type`; `0032` teaches the deceased lock to resolve those files. X-rays, scans and PDFs upload via `/api/procedures/[id]/attachments` into `Residents/<Name> (<ID>)/Procedures/<Type> <YYYYMMDD>/`, right after saving or later from the tab, and appear in the deceased archive.
- [x] **Single form for details + file upload.** Done 2026-09-21 for blood tests and procedures (see `docs/decisions.md`): the queue/progress/retry logic that `MaintenanceForm` had is now `useDeferredUploads()` + `FileDropZone` / `PendingFileList` / `UploadProgressPanel` in `src/components/DeferredUploads.tsx`, and `BloodTestForm` and `ProcedureForm` pick files alongside the details, upload them on Save and land on the tab. Still to do: **intake photos** — `IntakeForm` (`src/app/residents/new/IntakeForm.tsx`) has no picker; the photo route needs a folder category and a date-taken per file, and `recordIntake` redirects server-side rather than returning the id, so it's a different shape from the medical forms. Any future record-with-files form (projects, vet visits if they ever take files) should use the same hook. The sequential-upload constraint (Drive folder check-then-create isn't safe under concurrency) lives in the hook.
- [x] **Attach files to an existing blood test.** Done 2026-09-21: `BloodTestList` has the per-row "Attach files" toggle the Procedures tab has.
- [x] **Admin page for procedure types.** Done 2026-09-21: `/admin/procedure-types` (Admin nav, manual entry) lists each type with its procedure count and offers add / rename inline, delete blocked while any procedure references the row, and **Merge…** to fold a duplicate into the one to keep (`merge_procedure_type`, 0047 — one transaction, same shape as 0043's). Its own page rather than a combined one, see `docs/decisions.md`. Original brief: Staff and vets can add a type inline from the procedure form, but renaming one or merging a duplicate is SQL today. Same shape as `/admin/immunization-types`; could share a page with the medications/frequencies admin item below.

### Facility

- [x] **Copyable links for enclosure QR codes and resident RFID cards.** Done 2026-09-22 (see `docs/decisions.md`): the enclosure page and resident hub show the tag address with a **Copy link** button, and the enclosure cards and resident rows carry a copy icon for batch programming. The address is a short redirect — `/e/<id>`, `/r/<R-code>` — and a resident's serves visitors too: signed in → the hub, signed out → the public profile on `/adopt` if the resident is shown on the public site, otherwise sign-in and back. The proxy now remembers where a signed-out visitor was going (`?next=`) through both sign-in routes. Original brief: Customer request 2026-09-22. The shelter will put a QR code on every enclosure and a credit-card-sized RFID card outside each one showing the resident’s photo, name, age, sex and temperament; both are programmed with an external tool, so the system only needs to hand over the right URL. Add a **Copy link** control (with the URL shown next to it so it can also be selected by hand) on the enclosure page (`/enclosures/[id]`) and the resident hub (`/residents/[id]`), building the absolute URL with `getSiteOrigin()` from `src/lib/site-origin.ts` (it already resolves the real host behind Cloudflare and honours `NEXT_PUBLIC_SITE_URL`) so the link is always `https://lannacare.org/...` in production and never a localhost / IP address. Also add a **Links** column or per-row copy button on the enclosure list (`/enclosures`) and the residents list so a batch of tags can be programmed without opening each page. Decide (and record in `docs/decisions.md`) whether the link should be the plain hub URL or a dedicated short route (e.g. `/e/<id>`, `/r/<id>`) that redirects — a short route keeps the QR code low-density and survives a later page restructure. A scanner who is not signed in should land on the sign-in page and come back to the same URL afterwards (the redirect in `src/proxy.ts` and the `next` handling in `src/app/auth/callback/route.ts`). The card print itself (photo, name, age, sex, temperament) is out of scope here; if staff later want the system to produce the card artwork, that is a separate item.
- [x] **Staff feedback, round 1 (desktop look and feel).** Done 2026-09-21 from the first walk-through as a staff member (see `docs/decisions.md`). **Vet hub:** a 1-month period joins 3 / 6 / 12 / all time; the Residents seen card now says "Different residents · each seen once" or "· N repeat visits", since it only differs from Visits when a resident went more than once. **Maintenance:** staff and volunteers open the board on their own jobs (Assigned to: Me / Everyone; management and admin start on Everyone; a link naming a zone or enclosure shows everything there; `?assignee=me|all`); a job goes to a **team** — `0063_maintenance_teams_archived_users.sql` replaces `maintenance.assigned_user_id` with a `maintenance_assignees` join table, the form ticks people instead of picking one, cards read "Ann, Bo" or "Ann +2"; people who leave are **archived** from Admin → Security (`user_roles.archived_at`: `current_user_role()` returns null for them so nothing lets them in, the picker leaves them out, their name stays on past jobs with "(no longer here)" and the edit form keeps them ticked so they can be taken off; Restore undoes it). **Enclosures:** the three status buckets that matter for housing — Hospital, Unassigned, Fostered — sit at the top of the browser as cards without a zone heading (Adopted and Deceased are off it); each enclosure card's corner shows its open maintenance count in place of the zone name, and a zone heading shows its zone-wide open jobs as a link to the filtered board. **Residents list:** the Zone and Location cells are blank for a resident in a Lifecycle bucket instead of "Lifecycle · Internal". **Projects:** folder cards are a 16:9 strip with a smaller caption, six to a row on a wide screen. Contacts privacy (6a) is a separate open item under Management.
- [x] **Thai names for zones and enclosures.** Done 2026-09-21 as the paired-column pattern (`0058_place_names_th.sql`: `name_th` on `zones` and `enclosures`, carried through `resident_list_view` as `zone_name_th` / `enclosure_name_th`; Lifecycle pseudo-rows seeded with Thai names). `placeName(locale, name, name_th)` in `src/lib/enclosures/names.ts` is used at every render site — residents list and hub, placement forms and history, enclosure browser and hub, maintenance board / job page / forms, every zone → enclosure picker — with the English as fallback; Admin → Zones and → Enclosures edit the Thai name in place and take it on create. English stays the key for Drive, sorting and the status logic. Original brief: Asked for 2026-09-21 right after the translations feature landed: the staff who read the maintenance board and the resident hub in Thai see zone and enclosure names in English. These are labels, not prose — `zones.name` / `enclosures.name` are unique keys, Drive folder path components (`Projects/…/Enclosure Maintenance/<Zone>/<Enclosure>/`, resident folders), and the `Lifecycle` zone and its `Hospital` / `Fostered` / `Adopted` pseudo-enclosures are matched by name in code — so this is the paired-column pattern (`name_th`, as on `project_folders` and `residents.thai_name`), **not** the `translations` table. Migration: `name_th` on both tables (nullable), carried through `resident_list_view` (`zone_name_th`, `enclosure_name_th`) and every query that embeds `zones(name)` / `enclosures(name)`; Admin → Zones and Admin → Enclosures get a Thai name column edited in place. Display: one helper (say `placeName(locale, name, name_th)` in `src/lib/enclosures/`) used at every render site — 58 across 35 files as of today (`grep -rn "zone_name|enclosure_name" src`): the resident list and hub, enclosure browser and hub, pickers (`EnclosurePicker`, `loadEnclosureOptions`), maintenance board and job page, placement forms. English stays the key everywhere (sorting, Drive, matching); Thai is display only. Not on the public site (enclosures aren't shown there).
- [x] **Migrate the legacy AppSheet maintenance folders.** Closed 2026-09-21 without a migration: the user confirmed the only folders under the legacy `Enclosure Maintenance/<Enclosure>/Active|Completed/<date> (<id>)/Work to be Done/` tree are test folders, so there are no real jobs to read into `maintenance` rows and nothing to move. Original brief: read those folders into `maintenance` rows and move their files into the new layout, agreeing the mapping (enclosure names, what counts as Completed, the date) at a screen with the user and running once with a dry run first. Was (c) of the Maintenance follow-ups item; (a) and (b) closed the same day.
- [x] **Maintenance follow-ups (a) delete a job, (b) assign to a contact.** Done 2026-09-21. (a) **Delete job** on the job page (staff and above; confirm first): removes the attachment rows and the job, then the Drive folder — which takes the files with it — or, for a job that never had a folder, each file. Database first, Drive best effort, redirects to the board. (b) **Assigned to** on the log/edit form — corrected the same day to a **login**, not a contact (`0055_maintenance_assigned_user.sql`: `assigned_user_id` → auth.users replaces `assigned_to`; `app_users` view for the picker and names): the person who actions a job is always staff, a volunteer or management, never a carer or supplier. The assignee shows on the job page and on board cards. Original brief: (a) Deleting a job — none today, a mistaken job can only be edited; needs to remove its attachments and Drive folder. (b) Assigning a job to a contact — `maintenance.assigned_to` exists but isn't on the form; contacts now exist at `/contacts`, so the form can offer a picker (all types, not just carers).
- [x] **Enclosure maintenance.** Done 2026-09-20 (see `docs/decisions.md`): `0033_enclosure_maintenance.sql` adds `enclosure_id` (nullable — zone-wide jobs), estimated/actual cost, due date, `job_code` (M-0001), a cached Drive folder id and a before/after `phase` on `attachments`, and renames the statuses to Not Started / In Progress / Blocked / Completed. `/maintenance` is a drag-and-drop Kanban (desktop) / status list (phone), `/maintenance/[id]` has status buttons and Before / After photo sections, `/maintenance/new` uploads photos on the same form as the details. Files live in `Projects/Shelter Projects/Enclosure Maintenance/<Zone>/<Enclosure>/<Status>/<M-0001 Title>/` and the folder moves with the status.

### Admin

- [x] **Release notes register, with an email to admins on each major release.** Done 2026-09-23 (see `docs/decisions.md`, "Release notes register"): `src/lib/releases.ts` seeded with `0.0.1` "Current Baseline Build", `/releases` for every signed-in role (nav footer, manual topic), `package.json` at `0.0.1`; `scripts/deploy.mjs` refuses a production deploy with unreleased notes or a version mismatch, tags the Worker version, and mails a major release new to the site to that environment’s admins through the Worker’s free Cloudflare `send_email` binding (`worker/release-mail.mjs`, verified addresses only, `[UAT]`/`[Production]` in the subject, dev never sends). One-off Cloudflare setup in `docs/email-sending.md` §1; Resend SMTP for password reset stays under the Deployment item (§2). Original brief: Requested 2026-09-23 by Lutan. A page in the app (admin-visible; readable by everyone is worth asking) listing each release — version, date, **environment (UAT or Production)**, and short plain-language notes of what changed for users. Today nothing records a release: `package.json` is still `0.1.0` and deploys go through `scripts/deploy.mjs --env`, so the design needs to settle (a) **where notes live** — a checked-in file (e.g. `docs/releases.md` or a typed `src/lib/releases.ts`, written as part of the PR) is simplest and reviewable; a DB table (needs a schema PR first) allows editing in the app; (b) **how a release is recorded** — most naturally by `scripts/deploy.mjs`, which already knows the target env, stamping the version + env + date at deploy time; (c) **major vs minor** — a flag per release; only major releases send email. **Email:** on a major release, send the notes to every user with the `admin` role in that environment's database, with the environment named in the subject (`[UAT]` / `[Production]`) so a UAT test release is never mistaken for a live one. There is no app mailer yet — the only email today is Supabase Auth's (and production SMTP is still an open Deployment item), so pick a sender (a transactional API such as Resend, or the SMTP account set up for Auth) and note that `lannacare.org` Email Routing is receive-only. Depends on the `uat` environment item in Deployment for the UAT/Production distinction; until it lands, "dev" stands in for UAT. Record the choices in `docs/decisions.md`.
- [x] **Contacts follow-ups (a) notes, (c) maintenance link.** Done 2026-09-21: `0054_contact_notes.sql` adds `contacts.notes`, on the Management → Contacts add form and table (edit in place), shown on the contact hub under the details and included in the contact list's search. (c) moot since jobs are assigned to logins, not contacts (0055). Original brief: (a) A `notes` field (what a supplier sells, a volunteer's availability, a carer's home set-up) — the vets item has the same gap. (c) `maintenance.assigned_to` now has a contact page to link to once the maintenance form can set it.
- [x] **Vet hub follow-ups.** Done 2026-09-21: `0053_vet_cost_notes.sql` adds `vet_appointments.cost` (baht, ≥ 0) and `vets.notes`. Cost is entered on the new `/vet-visits/[id]/edit` page — the first way to change a visit after booking at all: status (now including Cancelled), date/time, vet, reason, notes, cost — reached from an Edit link on each row of the resident's Vet Appointments tab; the row shows the cost, and the vet hub gains a Spend card (total for the period, with how many of the visits carry a cost) and the cost on each visit row. Notes are on the Management → Vets form/table and under the contact details on the hub. Spend per resident and per month on the dashboard remain under Dashboard follow-ups (d). Original brief: (a) Visit cost — there is no cost column on `vet_appointments`, so the hub can't show spend per vet or per resident; add one (baht, like maintenance) and a total to the hub once invoices are being recorded. (b) A vet's `created_at` is the only "since when" we have; a `notes` field (specialities, opening hours, emergency line) would be more useful on the hub than in `contact_info` free text.
- [x] **Vets management.** Done 2026-09-20 (see `docs/decisions.md`); moved to `/management/vets` 2026-09-21 (`/admin/vets` redirects): the CRUD page for the existing `vets` table (name, clinic, contact info; delete is blocked while a vet has logged visits), and `/vets` → `/vets/[id]` is a read-only vet hub for every role — per-vet visit counts, residents seen, upcoming/overdue, linked procedures / blood tests / prescriptions, a visits-per-month chart and the visit list, over a 3 / 6 / 12 month / all-time window. Both in the nav. No migration.
- [x] **Contacts management.** Done 2026-09-20 (see `docs/decisions.md`); moved to `/management/contacts` 2026-09-21 (`/admin/contacts` redirects): the CRUD page for the `contacts` table (name, type, phone, email, LINE ID, Messenger, WhatsApp, address — 0036/0037; delete and a type change away from Carer are blocked while the contact has placements), and `/contacts` → `/contacts/[id]` is the phone-first contact list for every role — search, type chips, one-tap call / LINE / Messenger / WhatsApp / email / maps, and on the hub the residents currently with a carer plus their past placements. Assigning stays on the resident hub. Both in the nav.
- [x] **Project work (outreach projects) — file-system style.** Done 2026-09-20 (see `docs/decisions.md`): 0034/0035, `/projects` and `/projects/[id]`, categories seeded as locked root rows, photos on `attachments` (`owner_type = project`) with captions and a cover, Drive tree mirrored via `ensureProjectFolderPath()`. Original brief: The shelter does a lot of outreach — sterilisation drives, community feeding, school visits, rescue call-outs — and needs somewhere to keep photos and a short story about each one, for its own records and for the public site (see the "Our work" item under Public website). `project_folders` / `project_photos` (0001) exist and already model a tree (`parent_folder_id`, `drive_folder_id`, `top_level_category`) but carry no info: no title, date, story or public flag, and the RLS lets any volunteer write photos. **Shape:** the UI is a file browser. Root is `/Projects/`; the second level is a *fixed* set of categories — Shelter Projects, Community Projects, Community Outreach, Visitors and Volunteers, Social Media, Puppies, Sterilisations, Donations, Fundraising Campaigns, Events, Rescues, Miscellaneous (store as `top_level_category` with a check constraint or lookup; Shelter Projects already holds Enclosure Maintenance, see 0033, so the existing Drive folder must be reused, not recreated). Every level below that is user-defined through the UI: staff create nested folders to any depth, and any folder can hold photos *and* its own info card. **Migration:** add to `project_folders` `title_th`, `summary` / `summary_th` (short story, markdown), `project_date`, `location`, `is_public` (default false), `cover_photo_id` (FK to `project_photos`, nullable); add `caption` / `caption_th` and `sort_order` to `project_photos`; tighten RLS so volunteers can add photos to an existing folder but not create, rename, move or publish folders. **Drive:** mirror the tree exactly — `Projects/<Category>/<user folders…>/` — via `ensureProjectFolder()` in `src/lib/google/drive.ts` (create on demand, rename/move folder with the record), uploads through `/api/projects/[id]/photos` following the resident-photo route, registered in the photo-proxy lookup (0015/0019 pattern) so `driveImageUrl` serves them. **Pages:** `/projects` shows the category level; `/projects/[id]` shows a folder — breadcrumb path, subfolders first then a photo grid with captions, plus an info panel (title, date, location, story, cover-photo picker, "Show on website" toggle) and actions to new-subfolder / rename / move / upload. Newest-first and name sort, search across folder titles. Nav entry beside Enclosures. Bilingual fields follow the free-text strategy item under Quick wins — until that is decided, at least the public-facing title/summary/captions get Thai columns.

### Management


- [x] **Management role and dashboard.** Done 2026-09-21 (see `docs/decisions.md`): `management` is a fifth `app_role` (0038) with staff's operational access mirrored by RLS (0039) plus the Management section in the nav — `/management/dashboard`, `/management/contacts` and `/management/vets` (both moved from Admin). The dashboard covers the monthly report's figures (in care, adopted, fostered new/continuing, died, blood work in-house vs at a vet visit, initial vs follow-up vet visits, procedures by type — each with the names) for any month, a "right now" strip (in shelter / hospital / fostered / outreach, ready for adoption, vet visits due and overdue, open maintenance) and a 12-month intakes / adoptions / deaths chart. Admin sees everything management does.
- [x] **Move Vets management under Management.** Done 2026-09-21 on the user's call: `/management/vets` (`/admin/vets` redirects), with 0040 giving management read/write on `vets` — the one place management has more than staff. Website content stayed under Admin because only admins edit `site_content` (0018).
- [x] **Cashflow forecast: what the shelter is about to spend, in one place.** Done 2026-09-23 in two PRs. Schema first (`0071_cashflow_prices.sql`): `medication.cost_per_unit`, `immunization_types.cost` and `site_content.vet_visit_estimate`, all nullable so "not priced yet" is representable, edited in place on `/management/medications`, `/admin/immunization-types` and `/admin/website`. Then the feature: `0072_cashflow_forecast.sql` adds `cashflow_forecast(p_from, p_to)` returning one row per category per month — `category`, `month`, `amount`, `basis` (`priced` / `estimated` / `actual`) and `missing_prices` — built by calling `diet_forecast` (0051) and `medication_forecast` (0044) once per month rather than restating them (verified lossless against the whole-window figures). `/management/cashflow` is management + admin only: 30 / 90-day buttons plus the shared From/To picker, window total / average month / unpriced count, a stacked column chart (a sibling to `TrendChart.tsx`, no chart library) and the table it is drawn from, with a category toggle driving chart, table and totals together. Unpriced categories read "not priced yet" and link to where the price is entered; the page says on its face that it is committed outgoings, not a budget. See `docs/decisions.md` for the vet estimate, the gap-not-zero rule and the series colours. Follow-ups left open below: forecast vs actuals, the shared time-zone constant (Dashboard follow-ups (e)), CSV export, and 30/90 as the shared fixed windows.
- [x] **Make the "not priced yet" card on /management/cashflow actionable.** Done 2026-09-24. The card links straight to the pricing page when every missing price is in one category (`notPricedTarget()` in `src/lib/management/cashflow.ts`), and otherwise jumps to the table's "Not priced yet" row, which is highlighted on arrival and already links each category to its own page. With nothing missing it stays a plain read-out. Went out with CSV export (Cashflow follow-ups (c)).

### Auth

- [x] **Brand the Google sign-in screen.** Done 2026-09-22, as part of publishing the production sign-in app (Completed → Deployment): app name `Lanna Care for Animals`, home page `https://lannacare.org`, privacy `https://lannacare.org/privacy`, authorised domains `lannacare.org` + the Supabase host, published to *In production*, brand-verified the same day via the Search Console TXT record. Not recorded: whether the LCA logo was uploaded on the Branding page — if the account chooser still shows no logo, that is the one thing left. The Supabase Custom Domain add-on (paid) was not taken; the small-print domain still reads `supabase.co`. Original prompt: Google's account chooser currently says "to continue to qxkmhwybjggxvsfxsxbd.supabase.co". To show "Lanna Care for Animals" + logo instead: (1) Google Cloud console → Google Auth Platform → Branding: app name, LCA logo, support/developer emails, app home page, privacy policy URL, authorised domain(s) — the site domain plus `supabase.co`; (2) Audience → publish to *In production*; (3) submit for brand verification (basic `email`/`profile`/`openid` scopes only, typically a few days). Prerequisite on our side: a live `/privacy` page on the public website (and optionally `/terms`) that the Branding form can link to. Optional extra: Supabase *Custom Domain* add-on (Pro plan, paid) so the small-print domain reads e.g. `auth.<our domain>` instead of `supabase.co`. Remember to also add the production `https://<domain>/auth/callback` to Supabase → Authentication → URL Configuration → Redirect URLs, and the production login page won't work until that's done.
- [x] **Forgot password.** Done 2026-09-21: **Forgot password?** under the Sign in button → `/login/forgot` (public) → `resetPasswordForEmail` with a `redirectTo` of `/auth/callback?next=/account/password?reset=1`; the same "if that email has an account…" reply whether or not it exists. The callback now honours a same-origin `next` after the code exchange, and `/account/password?reset=1` is the existing change-password page in a third mode (heading says it came from a reset link; goes on to `/residents` after saving). **Production prerequisite** (README deploy step 5): a real SMTP provider in Supabase and the production callback in Redirect URLs — the built-in mailer is dev-only and rate-limited. Original brief: Add a "Forgot password?" link on login, a request page using Supabase `resetPasswordForEmail`, and a reset page that consumes the recovery link and sets a new password. Needs a production SMTP provider configured in Supabase.
- [x] **Access request queue on Admin → Security.** Asked for 2026-09-21 and done the same day: accounts with no `user_roles` row — a Google sign-in that was turned away, or a created login whose role insert failed — are listed under **Access requests** at the top of the page (name from Google where there is one, how they signed in, first seen, last attempt) with a role picker + **Approve** and a **Deny** that removes the account; the users table below shows only accounts with a role. No migration; the callback already kept the auth row. A public "request access" form for people without a Google account was considered and not built.
- [x] **Self-managed passwords: generated temp password + forced change on first login.** Done 2026-09-21 (see `docs/decisions.md`): Admin → Security no longer has a password field — creating a user mints a temporary password (`src/lib/auth/temp-password.ts`, 12 chars, no look-alikes) shown once with a Copy button, and the row's Reset became **Issue temporary password** (same, with a confirm; not for your own account). The account carries `app_metadata.must_change_password`; the request proxy sends such a user to `/account/password` and nowhere else while their session was opened with a password (the JWT `amr` claim) — a Google sign-in carries on. `/account/password` (also in the nav as Change password, for anyone) sets the new password as the user and then clears the flag via the service role. Google-only accounts just never use the temporary password. Original brief: Instead of the admin typing a password on Admin → Security, the app generates a temporary one (shown once to the admin to pass on), and the user must set their own password on first email/password login before reaching the app (flag on the account, e.g. `user_metadata.must_change_password`, checked in the session proxy / a `/account/password` page). Google sign-in bypasses the forced change since no password is used. Pairs with the Forgot-password item below so people manage their own passwords end-to-end and admins stop being in the password loop; the admin "reset password" action on the Security page then becomes "issue new temporary password". Consider making the password optional at creation for Google-only accounts.
- [x] **Sign in with Google.** Done 2026-09-20 (see `docs/decisions.md`); Supabase provider configured and verified working.

### Architecture

- [x] **Nothing catches a user-visible PR that forgot its `unreleased` line.** Done 2026-09-24 (see `docs/decisions.md`, "What counts as user-visible"). `docs/test-plan-template.md` §7 has a release-notes line, ticked or `n/a: <reason>`, and `scripts/check-test-plan.mjs` holds it to the diff: a tick with no new `unreleased` line fails, and a PR touching `src/app/`, `src/components/`, `src/lib/manual/`, `src/lib/i18n/` or `worker/` with no new line fails unless a plan answers the line; an `n/a` reason is printed in the CI output for the reviewer. Red flag, not a hard block, like the rest of `test-plan`. Original brief: Found 2026-09-24 while cutting `0.1.0`: `unreleased` in `src/lib/releases.ts` was **empty**, so the release notes for ten merged PRs had to be reconstructed from PR titles and diffs rather than written by the people who made the changes — which is the guesswork the register exists to prevent. #60 (cashflow forecast) and #67 (nav rework) both merged *after* the register landed in #62 and neither added a line, and both are plainly user-visible. `scripts/deploy.mjs` cannot catch it: it refuses production while `unreleased` is **non-empty** (so nothing ships undescribed), but an empty list is indistinguishable from "nothing user-visible shipped in this batch". The gap is one-directional and silent, and it gets worse the longer a release goes uncut, because the reconstruction is done by whoever cuts it from titles rather than intent. Fix: a line in `docs/test-plan-template.md` under Documentation — *"does this change something a shelter user would notice? if so, `src/lib/releases.ts`'s `unreleased` has a line for it, written for a user and not as a commit message"* — ticked or `n/a: <reason>` like everything else, so a PR that genuinely changes nothing visible says so out loud. Worth pairing with a check in `scripts/check-test-plan.mjs`: when a PR touches `src/app/` or `src/components/` and `unreleased` is unchanged, require that line to carry a reason rather than a bare tick, since that is exactly the combination that produced this. Not a hard gate on the diff — plenty of `src/app/` changes are genuinely invisible — but a prompt at the moment the author still remembers what they changed.
- [x] **Postgres has its own copy of the UTC "today" bug — `current_date` in a UTC session.** Done 2026-09-24 (see `docs/decisions.md`). `0073_shelter_today.sql` adds `shelter_time_zone()` / `shelter_today()` / `shelter_date(timestamptz)` and moves eight sites onto them: the `maintenance.date_created` default, the `date_completed` stamp, both lines of the death cascade, `public_shelter_stats` (`in_treatment` and both "this year" counts) and `public_recent_adoptions.adopted_on`. A death at 01:26 Bangkok now closes open courses on its own day, asserted in a rollback harness on dev. `medication_daily_requirement` had already been dropped by 0044; existing rows not repaired (dev: 0 affected). Cashflow month boundaries stay with Dashboard follow-ups (e).
- [x] **One command to run the three gates, printing each exit code.** Done 2026-09-24. `node scripts/gates.mjs` runs `npm run typecheck`, `lint` and `build` — the same three as CI's `check` job — every one of them even after a failure, and ends with `gates: typecheck=0 lint=0 build=0`, exiting 1 if any failed. It refuses to start (exit 2, nothing run) when `node_modules/.bin` lacks `next`, `tsc` or `eslint`. `--plan` adds `check-test-plan.mjs` as a fourth, opt-in because a correct plan is red until someone signs. Section 2 of the test-plan template now asks for its output pasted as-is.
- [x] **`worktree.mjs done` reports success when it has not removed the folder.** Done 2026-09-23 (see `docs/decisions.md`). `done` now checks, before touching anything, whether any process has the folder open (rename it to a sibling name and back: Windows refuses exactly when something holds it) and refuses, naming the holder. It removes the folder, prunes git's registry and deletes the branches, then verifies all four and prints `not done:` with what survived rather than `done`. `--stop-servers` ends dev servers running from the folder's `node_modules` and retries. `list` survives a stale or broken worktree (a `STALE` row instead of `fatal: not a git repository`) and names husks, which `done <name>` clears (build output only unless `--force`).
- [x] **`worktree.mjs list` should show which worktrees have a live session, and `done` should refuse one that does.** Done 2026-09-23. `list` has a `held` column (`HELD`/`free`, plus the Claude session's name from `~/.claude/sessions/<pid>.json`, checked against the live process's start time) and an `unpushed` column. `done` hard-refuses a branch with commits no origin ref has, with no override. It also *refuses*, rather than warns about, a held folder. The item asked for a warning because the check races. But Windows cannot delete a held folder anyway, so carrying on would only pull git out from under the session and leave a husk. The race (a session attaching in the milliseconds after the check) is the one case left.
- [x] **`sync` leaves the branch unpushed — the post-commit hook does not fire for merges.** Done 2026-09-23. `sync` pushes as its last step, and `.githooks/post-merge` pushes after every other merge or pull (including the daily `backlog` fast-forward). CLAUDE.md's "always matches" sentence is corrected and points at `list`'s `unpushed` column.
- [x] **`.claude/launch.json` hardcodes port 3000, so the browser pane is unusable from any worktree.** Done 2026-09-23. The file is gitignored and generated per checkout from `.port` (`dev` runs `npm run dev -- --port <port>`) by `new`, `dev`, `sync`, the post-merge hook and `node scripts/worktree.mjs launch`. Verified: `preview_start` with `name: "dev"` in the worktree-tooling worktree started `next dev --port 3002` and rendered the home page.

### Documentation

- [x] **In-app user manual, first English draft.** Done 2026-09-21 (see `docs/decisions.md`): `/manual` for every signed-in role, content in `src/lib/manual/en.ts`, layout in `src/app/manual/page.tsx`, screenshots in `public/manual/` generated by `node scripts/manual-screenshots.mjs` (opens Edge, you sign in, it captures every referenced screen). Written to get a feel for the shape; expect the prose to be reworked once real users have read it.
- [x] **Fix UI hints that still say "Admin → Contacts" / "Admin → Vets".** Done 2026-09-23 with the nav rework, which rewrote every "Admin →" menu path: the Contacts / Vets ones now say Management → (Thai: การจัดการ →), the rest Settings →. The manual screenshot re-run is part of the planned full re-run. Original brief: Spotted 2026-09-21 while writing the manual: contacts and vets management moved to `/management/*` that day, but several dictionary strings in `src/lib/i18n/dictionaries/en.ts` and `th.ts` still send people to Admin — e.g. `residents.rehome.carersOnlyHint`, `contacts.list.noContacts`, `contacts.hub.noDetails`, `contacts.hub.notACarer`, `vets.list.noVets`. Grep both files for "Admin →" and change each to Management (Thai equivalents in `th.ts`), then re-run the manual screenshot script since `resident-rehome.png` shows one of them.

### Public website

- [x] **Thai translation of home page content.** Done 2026-09-21 (see `docs/decisions.md`, "Public site pages"): the story moved into `site_pages` (0059) so its title and body go through the translations table and queue; tagline, hero alt and visiting hours are paired `_th` columns edited side by side on `/admin/website`. Original brief: `site_content` (tagline, story heading/body, hero alt) in Thai, picked by locale with English fallback on `/`. Since 2026-09-21 the way to do this is the translations table, not paired columns: add the columns to `translatable_fields`, attach the `queue_translations` / `drop_translations` triggers to `site_content`, expose `approved_translations('site_content', id)` wherever `/` reads it, and give the admin Website page a `TranslationPanel` per field — the manager queue then covers it for free. Short labels like the tagline could arguably stay paired columns (see the decision on `name_th`); decide per field.
- [x] **Visiting hours + adoption process on public pages.** Done 2026-09-21: `visiting_hours` / `_th`, `contact_phone`, `contact_line`, `contact_map_url` on `site_content` (0059); the `how-to-adopt` page in `site_pages` renders as a section at the foot of `/adopt` (`#how-to-adopt`); `/adopt/[id]` has the "Where to meet {name}" card with address, hours, email, LINE, phone and the can't-hold note linking to it. Original brief: Add `visiting_hours` (per-day text or JSON) and `adoption_process` (markdown/long text) to `site_content`, editable from `/admin/website`. On `/adopt/[id]` replace the bare mailto footer with a "Where to meet {name}" block (address from `contact_address`, hours by day, email/LINE contact) plus an honest note that residents may be adopted before a visitor arrives and can't be held over the phone, linking to a short "Our adoption process" section/page. Modelled on RSPCA ACT's pet profile footer.
- [x] **Filters on the public adoption listing.** Done 2026-09-21: species chips, size chips and a "Ready for adoption" toggle on `/adopt`, all URL search params, chips shown only for values present. Original brief: `/adopt` is a flat grid sorted by name. Add a species filter (Dog / Cat / Other, from the values in `public_resident_profiles`) and a "Ready for adoption" toggle, driven by URL search params so links are shareable and the page stays server-rendered. Keep it a simple chip/select row above the grid; no JS-heavy filter panel needed.
- [x] **"Similar residents" on the public resident page.** Done 2026-09-21: `loadSimilarResidents` (`src/lib/residents/public.ts`) — up to four same-species residents, ready first, shuffled — rendered with the shared `ResidentCard`. Original brief: At the bottom of `/adopt/[id]`, query up to four other rows from `public_resident_profiles` with the same species (excluding the current one, `ready_for_adoption` first, then random or by name) and render them as the same cards used on `/adopt`. Mirrors the "Similar animals to Harry" strip on RSPCA ACT.
- [x] **Adoption Recommendation fields on residents.** Done 2026-09-21 (0060, see `docs/decisions.md`): `good_with_dogs` / `_cats` / `_children` (Yes / No / Unknown), `energy_level`, `colour`, `is_desexed` on residents via one shared `AdoptionProfileFields` fieldset on intake and edit, summarised on the hub, and the "Is {name} right for you?" block plus Desexed / Vaccinated ticks on `/adopt/[id]`. Vaccinated is derived from `immunization_records`; no microchip field (not done in Thailand). Original brief: RSPCA ACT shows a separate "Adoption Recommendation" block (dog friendly / cat friendly / energy level / household activity / child recommendation) plus size and colour. Migration adding to `residents`: `good_with_dogs`, `good_with_cats`, `good_with_children` (nullable enum yes/no/unknown), `energy_level` (low/medium/high), `size` (small/medium/large), `colour` text. Expose the non-sensitive ones through `public_resident_profiles` (CREATE OR REPLACE VIEW — append columns only), add them to `/residents/[id]/edit` and the resident info tab, and render an "Is {name} right for you?" block on `/adopt/[id]`. Also surface Desexed / Vaccinated / Microchipped flags publicly if the data exists in the medical tables (otherwise add booleans on `residents`). These fields later enable richer `/adopt` filters.
- [x] **Foster, Volunteer and Donate public pages.** Done 2026-09-21: `/foster`, `/volunteer`, `/donate` from `site_pages` (0059) through one `SitePageView`, in `PublicHeader` (Donate as a button) and a "How you can help" strip on `/`; "Happy endings" strip on `/adopt` from `public_recent_adoptions` (0061). Also added while here: a shared `PublicFooter`, the "In vet care" tile now counts residents on medication or in hospital (0062) and a fourth tile for foster care. Original brief: Add `/foster`, `/volunteer` and `/donate` reachable from `PublicHeader` (and a "How to help" section on `/`). Also consider a **"Recently adopted"** page or strip (happy-ending stories): adopted residents are deliberately hidden from `/adopt` (0025), so this would read `placement_history` `Adopt` rows (say the last 90 days) joined to a public-safe resident view — name, photo, adoption month, no carer details — rather than the adoption listing views. Content lives in `site_content` (or a small `site_pages` table keyed by slug) so admins edit it from `/admin/website`; English + Thai columns following the Thai-translation item above. Foster page structure to borrow from RSPCA ACT: what fostering achieves, "what we need from you" list, "what we supply" list, then a CTA to email/LINE. Donate page holds bank transfer / PromptPay details and what donations fund.
- [x] **Share button and Open Graph tags on public resident pages.** Done 2026-09-21: `generateMetadata` on `/adopt/[id]` (photo as `og:image`, first sentence of the bio), `/adopt`, `/` (hero + tagline) and the three new pages; `ShareButton` uses the Web Share API with a clipboard fallback. Original brief: Add `generateMetadata` to `/adopt/[id]` (title = name, description = first sentence of bio, `og:image` = profile photo via `driveImageUrl`) so links preview correctly on Facebook and LINE, and a "Share {name}'s profile" button using the Web Share API with a copy-link fallback. Do the same OG treatment for `/` and `/adopt`.
- [x] **Shelter impact stats on the home page.** Done 2026-09-21 with the management dashboard (see `docs/decisions.md`): `public_shelter_stats` (0039) and a three-tile strip under the hero on `/` — in care, adopted this year (with last-7-days), in vet care. Original brief: Inspired by RSPCA ACT's "Last week at a glance" strip (residents in care / adopted last week / under vet care). Create a `public_shelter_stats` view (granted to `anon`) that derives counts from `resident_current_state` (Resident + Fostered + Hospitalised = in care; Hospitalised = under vet care) and from `placement_history` (`Adopt` placements with `start_date` in the last 7 days). Render three stat tiles with a one-line explainer each near the top of `/`. Public-safe: counts only, no names or IDs.
- [x] **Pet of the Week on the home page.** Done 2026-09-21 (see `docs/decisions.md`): `site_content.featured_resident_id` (0041), a single-select `ResidentPicker` on `/admin/website` fed from `public_resident_profiles`, and a card on `/` between the gallery and "Ready to meet everyone?" that is looked up through the same view, so it hides itself when the resident stops being public. Original brief: Add `featured_resident_id` (nullable FK to `residents`) to `site_content`, chosen via a resident picker on `/admin/website` (only publicly-visible residents offered). On `/` render a featured card (profile photo, name, species/breed, first paragraph of `bio`) linking to `/adopt/[id]`. Hide the section when unset or the resident is no longer public. Mirrors RSPCA ACT's "Dog/Cat of the week".
- [x] **"Our work" — project stories on the public site.** Done 2026-09-21 (see `docs/decisions.md`): 0042 adds the `public_projects` / `public_project_photos` views (anon SELECT only, checked by `check-public-views.mjs`), `/our-work` lists published folders as cards with URL-driven category chips, `/our-work/[id]` has the captioned gallery, story and Open Graph tags (cover as `og:image`), `PublicHeader` links Adopt / Our work, `/` shows the three newest under Pet of the week, and `/admin/website` lists what is published with a one-click "Remove from website". Original brief: Depends on the Project work item under Admin. Create a `public_projects` + `public_project_photos` view pair (granted SELECT only to `anon` / `authenticated`, following 0025: filtered on `is_public`, exposing title, summary, date, category, cover photo and captioned photos — never `uploaded_by` or Drive folder IDs). Public `/our-work` page listing projects as cards (cover photo, title, date, category, first line of summary; category filter via search params like the `/adopt` filters item) and `/our-work/[id]` with the story and photo gallery, linking from `PublicHeader` and a "What we do" strip on `/` showing the three most recent public projects. Pick title/summary/captions by locale with English fallback. Add `generateMetadata` (OG image = cover photo) so stories share well on Facebook and LINE, same as the resident-page item below. Once live, project stories are also the obvious content for the Foster / Volunteer / Donate pages ("here's what your donation funded").
- [ ] **Contact page map preview is too wide on desktop.** Customer request 2026-09-24. On a contact's page (`/contacts/[id]`), the Google Maps preview under the address overflows the screen width in desktop layout. The iframe is in `src/app/contacts/[id]/ContactHub.tsx` (`h-[200px] w-full`, inside a `min-w-0 flex-1` column beside the address icon) — find which ancestor lets it grow past the viewport (a flex/grid child missing `min-w-0`, or the iframe's intrinsic width), and give it a sensible cap (e.g. `max-w-md`/`max-w-lg`) so it sits under the address rather than spanning the card. Check phone width too, and both themes.

